/* conio.h - triage shim (OpenWatcom-style) for the §6a newlibc sweep.
 * minic's dos.h already declares kbhit/getch-family console helpers. */
#ifndef _CONIO_H
#define _CONIO_H
#include <dos.h>
#endif
