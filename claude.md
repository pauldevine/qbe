# Claude Session Status: QBE C11/C17 8086 Compiler

**Project:** C11/C17 + GNU-extensions C Compiler for 8086 DOS using QBE Backend
**Standard:** C11 feature set (`_Static_assert`, `_Generic`, `_Alignof`/`_Alignas`, compound literals, designated initializers, anonymous struct/union) + GNU extensions (`__attribute__`, inline `__asm__`, `__far` pointers).  Equivalently **C17-level** since C17 added no new language features over C11.  **No C23 language features.**  C only — no C++.
**Real target hardware:** Victor 9000 / Sirius 1 (~896 KB RAM) — NOT the IBM-PC 640 KB ceiling.  DOSBox is the fast loop for images that fit; MAME victor9k + SASI disk for the real thing.
**Last Updated:** 2026-06-13 (§7c)

---

## Where the project is (one paragraph)

The original compiler goal is **complete**, and so is the MicroPython hardening campaign that followed it.  All **six memory models work** (tiny .COM / small / medium / compact / large / huge — small was the last broken one, fixed 2026-06-11).  The per-model runtime gate is **289/289** (`tools/test-dos.sh`).  MicroPython (108 TUs) runs on a real Victor 9000: full language surface, FLOAT, the `math` module, a 114 KB split GC heap, an interactive REPL — all Victor-verified byte-exact vs host python3.  MicroPython's remaining feature tracks are **PARKED**; its role now is a **regression corpus** (rebuild compact, byte-compare the 731,088-byte body after toolchain changes; Victor re-runs only when bytes move).  Upstream QBE is in sync through `e786f06`.  Stevie ships as a 146 KB medium-model .EXE, interactively verified on the Victor.

## 🚧 Next frontier: newlibc for the Victor 9000 (Phase 6, designated 2026-06-11)

Compile **`~/projects/newlibc`** — a much-progressed Victor 9000 C library + driver suite (bare-metal newlib port: crt0, libgloss, display/keyboard/timer/SASI drivers, VFS + read-only FAT, ~40 MAME-driven tests; phase 1 DOS drivers built with OpenWatcom, phase 3 bare-metal built with ia16-elf-gcc) — with **this** toolchain, and adopt its test suite as a standing robustness harness.  End state: newlibc replaces libstub as the real libc.  **Step 1 (triage sweep) DONE 2026-06-11 (§6a)**: 46/66 phase-3 TUs compile under small AND medium after seven minic dialect fixes.  **Step 2 (DOS-hosted portable subset) DONE 2026-06-11 (§6b)**: ELEVEN newlibc tests (snprintf, six FAT/VFS, ramfs, stdio_route, bss, terminal_meta) run DOS-hosted through the full newlibc stack and are standing gate entries (`tools/build-newlibc-test.sh`, `minic/dos/newlibc/`, `libstub_to_exe.py --no-stdio`); two toolchain bugs fixed en route (static file-scope data linkage; decimal `UL` literal typing).  **Step 3 (bare metal) DONE 2026-06-11 (§6c)**: the toolchain's first bare-metal program runs on the Victor 9000 under MAME — `omf_link.py --raw-binary --load-addr 0x3000` (link-time-absolute selectors + a synthesized register-setup stub at the image head), minic-built crt0 + polled NEC-7201 serial console (`minic/dos/newlibc/bm_*.c`, pure volatile-far MMIO, no inline asm), `hello_bm` PASS over serial (`tools/build-newlibc-baremetal.sh` / `tools/run-victor-baremetal.sh`; gated in `test_omf_link.sh` + `tools/test-victor.sh`).  **Step 4a (ISR ABI) DONE 2026-06-11 (§6d)**: `__attribute__((interrupt))` is a real compiler feature — QBE `interrupt` function linkage → the i8086 backend emits the ES-safe ISR prologue/epilogue itself (static CS-local ES save, all-register save, DS/ES=DGROUP via a link-time `dw DGROUP` word, iret); gated by `isr_probe` (small+medium) and by `timer_bm` taking LIVE 8253-ch2/IR2 timer interrupts through a minic-built ISR on MAME victor9k (victor pipeline 4/4; `bm_pic.c` full 8259A re-init is MANDATORY before any bare-metal `sti` — the boot ROM leaves IRQs unmasked on stale vectors).  **Step 4b (driver suite + battery) DONE 2026-06-12 (§6e)**: display (6845 + font RAM + glyph-pointer VRAM), keyboard (VIA-CS2 state machine, interrupt-driven on IR6), and a REAL serial-RX ISR (7201 channel B, IR1, drain-to-empty for the edge-triggered PIC) all run bare-metal through compiler-emitted ISRs — three new MAME-verified tests, each PASS on the first run, zero compiler changes; `tools/test-newlibc.sh` is the standing bare-metal battery (5/5: hello, timer, display, keyboard, serial), with harness keystroke/serial injection (`V9K_KEYPOST` via natkeyboard, `V9K_SERIAL_IN` via a mid-run-attached rs232b null_modem).  **The step-4 extended-asm question is ANSWERED: not needed** — every remaining driver inline-asm site was ia16-gcc ES damage control (the ISR ABI owns ES) or a pushf/cli flags-save the single-producer ring design obsoletes.  **Step 4c (battery sweep) DONE 2026-06-12 (§6f)**: the four "near-free" phase-3 tests ported and standing — `memory_bm` (font/screen RAM write-readback), `crtc_bm` (raw-VRAM pattern + R14/R15 cursor readback), `pic_bm` (IMR get/set + gating proof with the timer live; new `bm_pic_get_mask`/`set_mask`), `interrupt_bm` (the cross-driver stress: display init + 60 scrolling lines under the live timer ISR; needs a 120-emulated-second budget) — battery **9/9**, every port first-run PASS, zero compiler changes again.  **Step 4d (cooked console) DONE 2026-06-12 (§6g)**: `bm_tty.c/h` — the minic port of newlibc's `console_dev_read`/`console_dev_write` pair (output mirrored to display + serial; cooked keyboard input with Backspace/DEL rubout, CR→LF, blocking line reads) — the stdio seam for retiring libstub's INT 21h stdio; the `tty_bm` test types `vx\b9k\nz` through MAME's natural keyboard (a REAL Backspace keystroke through the full ISR path) and verifies the edited line in the buffer AND in VRAM — first-run PASS, zero compiler changes (third driver session running on the §6d ISR ABI); harness `V9K_KEYPOST` is now byte-safe (every byte → Lua `\ddd`, so control chars type real Victor keys: `\b`=Backspace, `\n`=Return), battery **10/10**.  **Step 4e (stdio over bm_tty) DONE 2026-06-12 (§6h)**: printf()/fgets() run on the bare machine through the REAL newlibc stack — the seam is **newlibc's VFS `/dev/console` routing moved bare-metal** (not a libstub-level swap): `bm_shim.c` is dos_shim.c's bare-metal parallel (console_dev_* → bm_tty, timer → bm_timer, display → bm_display, POSIX aliases + FILE layer), `bm_stdio.h` is the build probe pulling the full portable-subset TU set (printf/scanf wrappers, syscalls, vfs, fat, block — zero `#ifdef DOS` anywhere), and the `stdio_bm` battery test proves the whole path (printf format sweep, write(1), /dev/null fprintf, ramfs fread, isatty, `fgets(stdin)` returning `"v9k\n"` from a keypost with a REAL Backspace edit, getchar, times()) — FIRST-RUN PASS, zero compiler changes, battery **11/11**; upstream newlibc moved again (FAT WRITE + SASI WRITE(6)) with NO golden impact (the write support is a runtime-installed dispatch table we don't link).  **Step 4f (bare-metal disk I/O) DONE 2026-06-12 (§6i)**: the minic-built SASI/Xebec driver (`bm_sasi.c/h`, name-for-name port of newlibc's `drivers/sasi.c` — both upstream inline-asm constructs DROPPED, not translated: SAVE_ES was ia16-gcc damage control and pushf/cli is unnecessary under a REQ-driven handshake, proven by running every transfer with timer+keyboard ISRs live) reads and writes real sectors on MAME's `-scsi:0` hard disk, and the UNMODIFIED newlibc block/FAT/VFS stack mounts Victor virtual volume 0 from them (first minic coverage of `vfs_mount_victor_fat`'s disk-label path) — the `sasi_bm` battery test (geometry, LBA-0 label byte-exact vs host xxd, checksum repeat-read, stat/open/fgets of CONFIG.SYS, WRITE(6) round-trip @ LBA 59057 on the harness's scratch copy) was a FIRST-RUN PASS with zero compiler changes (fifth straight driver session on the §6d ISR ABI); harness grew `V9K_HARD_DISK` (scratch-copy attach) and a per-test disk field; battery **12/12**.  **Step 4g (DOS-hosted tests re-run bare-metal) DONE 2026-06-12 (§6j)**: the ten portable DOS-hosted newlibc tests (snprintf, stdio_route, the six FAT/VFS, terminal_meta, ramfs) now run UNMODIFIED on the bare machine through bm_stdio — `build-newlibc-baremetal.sh` grew a **test-host mode**: a source resolving into newlibc's `tests/` directory gets the same `-Dmain=newlibc_test_main` rename the DOS gate uses, with new `minic/dos/newlibc/bm_testhost.c` (driver bring-up in the §6d interrupt-window order → `vfs_init()` → call the test → result line + `__V9END__`) as `main()` and the full bm_stdio TU set linked; each test's bare-metal serial output is **line-identical** to its DOS-hosted golden between the testhost preamble and result line (the ten new goldens are `minic/dos/tests/<name>.golden.txt`).  All ten were FIRST-RUN PASS, zero compiler changes (sixth straight session on the §6d ISR ABI); battery **22/22** (`test-newlibc.sh` grew the ten entries; ramfs_test needs a 300-emulated-second budget for its 103 output lines per the §6f scroll lesson).  `bss_test` stays DOS-hosted only (display-only output + a hlt idle loop; `memory_bm` already covers bare-metal RAM).  Upstream newlibc moved again (mkdir/rmdir/rename + the FAT-write commits now merged through PR #19) with NO golden impact — `test-dos` still 289/289, the write support remains a runtime-installed dispatch table we never link.  **Step 4h (FAT WRITE, DOS-hosted half) DONE 2026-06-13 (§6k)**: the UNMODIFIED upstream `fat_write_test.c` now PASSES through the full newlibc FAT WRITE layer (`vfs_mount_fat_rw` + create/multi-cluster-write/read-back/append/truncate/unlink/mkdir/rmdir/rename over a RAM disk) — and doing it required the **first MEDIUM-model DOS-hosted newlibc port**, because `fat_write.c` (~18 KB code) over the FAT/VFS/stdio stack overflows the small model's single-`_TEXT` 64 KB code ceiling in EVERY host (measured: bare-metal 81 KB, DOS upstream 77 KB, a stripped minimal DOS test still 68.6 KB; the floor `fat.c`+`fat_write.c`+`vfs.c` is irreducible).  Medium is far CODE (code splits across per-TU `<=64 KB` CS segments) + near DATA (one DGROUP; minic does NOT `far_stdlib`-mangle under `NEAR_DATA()`, which includes medium, so newlibc stdio links by real name under `--no-stdio`).  Two over-conservative guards lumping medium with far-DATA were relaxed (`build-newlibc-test.sh` small→small+medium + `--stack-size`; `libstub_to_exe.py --no-stdio` `near_code`→new `near_data_model`).  **One real compiler/toolchain bug fixed**: `asm_to_omf.py` split a relocatable `.long _sym` initializer into `dw off + dw seg` only for far-DATA models; in medium every `.long _sym` is a 4-byte far CODE pointer (function-pointer field in a static device-ops table — `console_device.write`) but fell through to `dd` (offset only, **segment word 0**), so the indirect far CALL through it jumped to seg 0 → hang (`split_sym_long = far_data or model == 'medium'`, scoped to medium to keep the compact/large/huge corpus byte-identical).  `dos_shim` gained the write side of its FILE layer (`fwrite`; `fopen` `"w"/"a"/"+"` mode→`O_CREAT/O_TRUNC/O_APPEND/O_RDWR`).  Gated as `newlibc medium (fat_write_test)` in `test-dos.sh`; **test-dos 290/290**, all 11 small newlibc tests still golden-identical, **MP compact body 731,088 bytes byte-identical** (compact provably unaffected), stevie (medium) still builds — no `emit.c` change.  **Step 4h (FAT WRITE, bare-metal half) DONE 2026-06-13 (§6l)**: the bare-metal `fatwrite_bm.c` now PASSES on the real `-scsi:0` SASI disk through the full newlibc FAT WRITE layer — the toolchain's **first MEDIUM-model BARE-METAL program** (88 KB code, over the small single-`_TEXT` 64 KB ceiling, confirming medium multi-CS is mandatory on bare metal too).  **FIRST-RUN PASS, ZERO compiler/toolchain changes** — everything medium needs was already in place from §6k (the `asm_to_omf` far CODE-pointer static-init fix, `NEAR_DATA()` covering medium, the `libstub_to_exe` `near_data_model` guard); omf_link's `--raw-binary` path is natively medium (multi-CS, resolves the entry CS:IP for the stub unchanged), `bm_crt0.c` far-calls `main` automatically under `minic -m medium`, and the §6d ISR ABI works unchanged (the timer ISR's far function pointer carries its real CS; `iret` is model-independent).  The only changes were build/harness plumbing: `build-newlibc-baremetal.sh` gained `--model=small|medium`, and `test-newlibc.sh` an optional sixth `:<model>` field (`fatwrite_bm:240:::hd:medium`, the §6i `V9K_HARD_DISK` scratch-copy path).  Phase 8's multi-cluster SASI write on the 5 MHz 8088 dominates the budget (240 emulated seconds — 90 truncated mid-write, the §6f slowness lesson, NOT a hang).  No toolchain change → no emit audit, no MP byte-compare.  Step 4h is now COMPLETE on both halves.  **§6m (2026-06-13) added a SECOND medium gate, both halves, zero toolchain changes**: the UNMODIFIED upstream `fat_write_unit_test.c` (FAT16 entry write/read + both-FAT mirroring + cluster-chain alloc/free + create/write/truncate/unlink/mkdir/rename + ENOSPC, and FAT12 entries straddling a FAT sector boundary in both parities — exercising the `fat_write.c` primitives DIRECTLY on hand-built RAM volumes, so no SASI) now PASSES DOS-hosted (`test-dos` **291/291**) AND bare-metal through bm_stdio test-host mode (`fat_write_unit_test`, RAM-disk only → no `-scsi:0`; battery **24/24**) — FIRST-RUN PASS on both, same medium support from §6k.  The only changes were build/harness plumbing: `build-newlibc-baremetal.sh` gained `--stack-size` (mirroring the DOS build; the test's hand-built RAM-volume `media[]` arrays + the full bm_stdio driver set push data+bss to 60,688 B, so the default 8 KB stack overflows the 64 KB DGROUP — it runs at 4096), and `test-newlibc.sh` grew an optional seventh `:<stack>` entry field (the entry-parser was rewritten to clean `IFS=:` field-splitting, verified equivalent for all existing entries).  **§6n (2026-06-13) gated the two keyboard-input tests DOS-hosted, zero toolchain changes**: `stdin_test` (getchar/fgets) and `scanf_test` (scanf) — parked since §6k for want of a harness input channel — now run small-model through a **DOS stdin redirect** (`PROG < IN.TXT`; on DOS, INT 21h AH=3Fh on handle 0 reads the file raw, no echo, so the run is deterministic).  Plumbed backward-compatibly through three layers: `run-dos-exe.sh` `$DOS_STDIN` env var; `run-dos-batch.sh` optional 3rd TAB manifest field (host stdin file → staged `Tnnnn.IN` → `Tnnnn.EXE < Tnnnn.IN > Tnnnn.TXT`; 2-field legacy lines parse with an empty 3rd field, command byte-identical); `test-dos.sh` `stage_runtime_case` optional 4th arg threaded into that field.  Fixtures + redirect-captured goldens in `minic/dos/tests/newlibc_{stdin,scanf}_test.{stdin,golden}.txt`; **test-dos 293/293**, no compiler source touched → no emit audit, no MP byte-compare.  **§6o (2026-06-13) drove those SAME two keyboard-input tests BARE-METAL, zero toolchain changes**: `stdin_test` and `scanf_test` now run small-model through the cooked `bm_tty` console (interrupt-driven keyboard on IR6) as standing battery entries — `getchar`/`fgets`/`scanf` reach `_read(0,…)` → `console_dev_read` → `bm_tty_read` (`getchar` reads ONE keystroke, count=1, no Enter needed; the line readers stop at the echoed Return), all fed by a single `V9K_KEYPOST` natkeyboard burst into the IR6 ring so timing is irrelevant — except a throwaway char AFTER the final `\n` is REQUIRED to commit that Return into the ring before the program blocks on it (the §6h `stdio_bm` flush lesson; entries `stdin_test:35:Ahello\nz` and `scanf_test:35:victor 42\nz`).  Both were FIRST-RUN PASS on MAME (getchar='A' / fgets="hello\n"; %15s="victor" / %d=42); the new goldens `minic/dos/tests/{stdin,scanf}_test.golden.txt` differ from §6n's DOS-redirect goldens because the cooked console ECHOES the typed input, and they build small-model (no `fat_write.c` bulk).  Only `test-newlibc.sh` + the two goldens changed → no emit audit, no MP byte-compare; **battery 26/26**.  **§6p (2026-06-13) ran three UNMODIFIED upstream SASI-backed FAT tests bare-metal on the REAL `-scsi:0` disk, zero compiler/toolchain changes — battery 29/29**: where §6l's `fatwrite_bm` was a hand-mirrored minic TU, this runs the upstream tests THEMSELVES through bm_testhost + the full bm_stdio/VFS/FAT stack — `sasi_fat_smoke_test` (read CONFIG.SYS, small, 64,771 B code), `sasi_fat_dir_test` (root + subdir iteration via `vfs_mount_victor_fat`; adds `dirent.c` → 66,435 B overflows small's 64 KB `_TEXT` → MEDIUM, a read-path re-confirmation of the §6k/§6l ceiling lesson), and the headline `sasi_fat_write_test` (create/write 2000 B / read-back / append / unlink on the real disk via `vfs_mount_victor_fat_rw` + SASI WRITE(6), CONFIG.SYS intact before+after; MEDIUM, 240-s budget for the Phase-8 multi-cluster write).  All three FIRST-RUN PASS against a scratch copy of `victor_30mb.img` (`hd` field → `V9K_HARD_DISK`).  These run bare-metal ONLY (the DOS host has no raw SASI), so their goldens are captured from the bare-metal run, not DOS-diffed.  The lone change was widening `build-newlibc-baremetal.sh`'s SASI TU probe from `bm_sasi\.h` to `sasi\.h` (the upstream tests `#include "sasi.h"`; `bm_sasi.h` is a byte-for-byte port and `bm_sasi.c` the only SASI impl we link) — no compiler/qbe/emit source touched.  Entries `sasi_fat_smoke_test:60:::hd`, `sasi_fat_dir_test:90:::hd:medium`, `sasi_fat_write_test:240:::hd:medium`; verified `[ok] [ok] [ok]` through the battery harness.  **§6q (2026-06-13) ran the UNMODIFIED upstream `sasi_sector_test` raw-block probe bare-metal on the real `-scsi:0` disk, zero compiler/toolchain/build-script changes — battery 30/30**: the read-only block-layer counterpart to §6i's hand-mirrored `sasi_bm` (and to the §6p FAT family — runs the upstream test ITSELF) — registers the SASI block device, reads LBA 0 twice with a `block_cache_invalidate` between, checks the two checksums match (0x8DDD), dumps the first 32 bytes (the `tandon_703_mame` label, no boot-sector signature) and the SASI bus/diagnostics (geometry 59058×512).  FIRST-RUN PASS against a scratch copy of `victor_30mb.img`.  **Model lesson re-confirmed a third time:** "read-only" ≠ small-model — 65,577 B code in small is 41 B over the 65,536 `_TEXT` ceiling (wraps and hangs, the §6p `sasi_fat_dir_test` symptom), so MEDIUM (70,944 B multi-CS); reads LBA 0 only → 60-s budget enough.  Needed NOTHING new: §6p already widened the SASI TU probe to `sasi\.h` (the test `#include`s both `block.h` and `sasi.h`), so the lone diff is one battery entry (`sasi_sector_test:60:::hd:medium`) + one bare-metal-captured golden → no emit audit, no MP byte-compare.  **§6r (2026-06-13) gated the UNMODIFIED upstream `fat_victor_label_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 294/294, battery 31/31**: unlike the §6i/§6p SASI tests (raw `-scsi:0`, bare-metal-only) this is RAM-disk style — it hand-builds a Victor drive-label + volume-label + FAT12 in a `media[]` array via `block_register_ramdisk`, so it ALSO runs DOS-hosted (added to `NEWLIBC_TESTS` in `test-dos.sh`) and its bare-metal serial output is line-identical to the DOS golden between the bm_testhost preamble and the result line (the §6j RAM-disk pattern).  It is the **first deterministic golden for the Victor drive-label → volume-label → relative-data-start parse path** (`fat_mount_victor` / `vfs_mount_victor_fat`): the other RAM-disk FAT tests use standard BPB mounts, and this path was previously covered ONLY bare-metal-on-real-SASI (§6i, §6p).  Builds SMALL in both hosts (DOS 52,003 B; bare-metal 60,697 B, under the 64 KB `_TEXT` ceiling — no medium, contrast the §6p/§6q read-path tests).  FIRST-RUN PASS on MAME; battery entry `fat_victor_label_test:60:::`, goldens `minic/dos/tests/{newlibc_,}fat_victor_label_test.golden.txt`; the DOS golden verified byte-exact through the run-dos-batch path.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6s (2026-06-13) gated the UNMODIFIED upstream `block_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 295/295, battery 32/32**: the block-device layer tested DIRECTLY one level below FAT — `block_register_ramdisk` + `block_init`, single/multi-sector read, write-through cache + `block_cache_invalidate` refresh, `block_get_info`/`block_status`/`block_cache_flush`, and the EINVAL/ENODEV/EROFS error paths, all over RAM-backed media (no `-scsi:0`).  Like §6r's `fat_victor_label_test` it is RAM-disk style, so it runs BOTH DOS-hosted (added to `NEWLIBC_TESTS` in `test-dos.sh`, golden byte-exact through run-dos-batch at 295/295) AND bare-metal (battery entry `block_test:60:::`, body line-identical to the DOS golden between the bm_testhost preamble and result line).  This is the **first deterministic golden for the block layer in isolation** — the FAT tests reach it transitively but never assert its cache/error semantics.  Builds SMALL in both hosts (DOS 51,811 B; bare-metal 60,505 B, under the 64 KB `_TEXT` ceiling — a RAM-disk block-layer TU set is leaner than the §6p/§6q SASI read-path tests that pulled dirent.c over the ceiling).  FIRST-RUN PASS on MAME and through the DOS gate; goldens `minic/dos/tests/{newlibc_,}block_test.golden.txt`.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6t (2026-06-13) gated the UNMODIFIED upstream `read_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 296/296, battery 33/33**: the raw `read(0,…)` keyboard-input layer, the third member of the §6n/§6o keyboard family after `stdin_test` (getchar/fgets) and `scanf_test` (scanf).  It exercises `read(STDIN_FILENO,&ch,1)` (one byte) and `read(STDIN_FILENO,line,39)` (a cooked line) DIRECTLY — asserting the syscall byte count, that the buffer stops at the newline, and that it carries no `\b` byte — coverage the wrapper tests reach only transitively through the same `_read(0,…)` path.  DOS-hosted via the §6n stdin redirect (`< IN.TXT`, AH=3Fh raw on handle 0, no echo): fixture `Ahello\n` (no Backspace — the raw redirect performs no rubout), added to the §6n loop in `test-dos.sh` (`stdin_test scanf_test read_test`), golden verified byte-exact through run-dos-batch.  Bare-metal via the §6o cooked `bm_tty` console: entry `read_test:35:Avx\b9k\nz::` — `read(0,&ch,1)`='A', `read(0,line,39)` reads `vx\b9k` cooked → a REAL Backspace rubs out the `x` → "v9k\n" (4 bytes), trailing `z` flushes the final Return into the IR6 ring; golden ECHOES the input (vs the no-echo DOS golden), same §6n/§6o divergence as stdin/scanf.  Builds SMALL both hosts (DOS 51,117 B; bare-metal 59,811 B — portable stdio, no fat_write.c bulk).  FIRST-RUN PASS on MAME and through the DOS gate; goldens `minic/dos/tests/{newlibc_,}read_test.golden.txt` + fixture `newlibc_read_test.stdin.txt`.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6u (2026-06-13) gated the UNMODIFIED upstream `driver_test` bare-metal through bm_testhost, zero compiler/toolchain/build-script changes — battery 33/33 → 34/34** (test-dos UNCHANGED 296/296, a bare-metal-only gate like §6q): the **first DRIVER-layer upstream test** (§6j–§6t ran upstream tests over the portable stdio/vfs/fat/block surface; the drivers were covered only by the hand-mirrored `bm_*` ports).  It validates the Phase-1 hardware-fix story against the LIVE bm_timer/8259 — Test 1 measures a real 100 ms `timer_delay_ms` and asserts ~10 ticks (deterministic 10 in MAME: both `timer_get_ticks()` and `delay_ms`'s start read the same ISR `tick_counter` on consecutive instructions, no tick between), Test 2 asserts `timer_get_frequency()==100`, Test 4 reads the live 8259 IMR through the `PIC_GET_MASK()` MMIO macro (direct `volatile __far` read of E000:0001 → 0xBB, IR2 unmasked), all five tests print fixed text + PASS and `main` returns 0.  The driver surface resolves through `bm_shim.c` (`timer_*` → `bm_timer`, `timer_get_frequency` → `100UL`) + the `v9k_hw.h` PIC/timer MMIO macros — NOTHING new to link, so like §6q the only changes are one battery entry (`driver_test:90:::`) + one bare-metal-captured golden (`minic/dos/tests/driver_test.golden.txt`).  Bare-metal ONLY (the DOS host has no live 8253/8259 → the measured-delay and live-IMR lines have no DOS golden).  Builds SMALL (59,485 B code, under the 64 KB `_TEXT` ceiling); 71 output lines at the §6f display-scroll rate need a 90-emulated-second budget (60 s truncates mid-Test-5).  FIRST-RUN PASS on MAME; verified `tools/test-newlibc.sh driver_test` → [ok] (the harness diffs live serial against the golden, so [ok] IS the gate).  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6v (2026-06-13) gated the UNMODIFIED upstream `simple_interrupt_test` bare-metal through bm_testhost, zero compiler/toolchain/build-script changes — battery 34/34 → 35/35** (test-dos UNCHANGED 296/296, a bare-metal-only gate like §6q/§6u): the minimal CONTINUOUS-timer-interrupt test (33-line upstream TU: read a start tick count, then 5× `timer_delay_ms(1000)` each printing elapsed ticks, then an UNCONDITIONAL `PASS: Interrupts working!`).  Complements §6u's `driver_test` (which measured a SINGLE 100 ms delay): this proves 5 seconds of CONTINUOUS timer interrupts keep the ISR `tick_counter` incrementing monotonically under the full bm_stdio/bm_timer stack — the longest sustained-interrupt run in the battery.  Resolves entirely through `bm_shim.c` (`timer_get_ticks`/`timer_delay_ms` → `bm_timer_*`) — NOTHING new to link, so like §6q/§6u just one battery entry (`simple_interrupt_test:30:::`) + one bare-metal-captured golden.  **Chosen over the larger `interrupt_test`, which is UNSUITABLE**: its Test 1 reads `start_ticks` BEFORE four slow display-mirrored printfs, so accumulated scroll ticks push elapsed past its `[90,110]` window → it prints `FAIL` (wrong to gate), and its Test 3 embeds a raw busy-loop iteration count.  **Note on the golden's tick values** (`Start: 111`, elapsed `155/316/476/637/797`): MAME models the Victor channel-2 clock FASTER than the nominal 100 Hz (the upstream comment warns of this) and the display-mirrored printf between delays adds ~61 ticks, so elapsed grows ~161/iteration — these are DISPLAY-SCROLL-TIMING-derived, RUN-STABLE (cycle-deterministic, verified byte-identical across three runs) but they WILL SHIFT if a bm_tty/printf codegen change alters timing → re-capture then (the PASS verdict is unconditional, so a shift is a LOUD diff, never a silent wrong pass).  Bare-metal ONLY (no live 8253 on DOS); SMALL (58,723 B code); 30-s budget.  FIRST-RUN PASS on MAME, verified `tools/test-newlibc.sh simple_interrupt_test` → [ok].  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6w (2026-06-13) gated the UNMODIFIED upstream `keyboard_raw_test` bare-metal through bm_testhost — battery 35/35 → 36/36** (test-dos UNCHANGED 296/296, bare-metal-only like §6q/§6u/§6v): the **first gate of the RAW (uncooked) keyboard event nonblock API** (the §6e `keyboard_bm` and the §6n/§6o/§6t `stdin/scanf/read` family all exercise the COOKED `bm_tty` path; this calls `keyboard_get_raw_event_nonblock()` directly against the interrupt-driven IR6 event ring beneath the cooking).  Run with **NO keypost** so the raw ring stays empty → after a 500-tick (5 s) idle window it takes the `count==0` branch and prints `PASS: no raw keyboard events arrived during idle check.` + `return 0`.  Driving with no keypost SIDESTEPS the keypost-vs-poll timing race the keyboard tests were parked on (no keys → no race; idle branch deterministic), and the idle branch prints **ONLY fixed text — no tick values** → the golden is fully toolchain-stable (strictly better than §6v's timing-derived ticks).  **One change, build-glue only**: `bm_shim.c` gained keyboard surface aliases (`keyboard_get_raw_event_nonblock`/`keyboard_hit`/`keyboard_getc`/`keyboard_getc_nonblock` → `bm_keyboard_*`, mirroring the file's existing `timer_*`/`display_*` surfaces) so the unmodified test links its unprefixed names — bm_keyboard.c is already linked into every bm_stdio build and `bm_tty_init()` already inits the IR6 ISR, so nothing NEW links, only wrapper symbols (the aliases also pre-stage `keyboard_nonblock_test`).  This is a newlibc bare-metal support TU (NOT compiler/qbe/emit/minic; MP does not link bm_shim.c) → **no emit audit, no MP byte-compare**.  Builds SMALL (59,223 B code); 30-s budget; bare-metal ONLY (no live IR6 ring / 8253 on DOS).  FIRST-RUN PASS, verified `tools/test-newlibc.sh keyboard_raw_test` → [ok]; additive aliases confirmed non-disturbing by re-running `stdin_test`/`stdio_bm`/`snprintf_test` → all [ok].  **§6x (2026-06-13) gated the UNMODIFIED upstream `keyboard_nonblock_test` bare-metal through bm_testhost — battery 36/36 → 37/37** (test-dos UNCHANGED 296/296, bare-metal-only): the nonblock-COOKED-byte pair one layer above §6w's raw-event API — `keyboard_getc_nonblock()` (Test 1, no key pending → `< 0` → "OK: no key was pending.") and `keyboard_hit()` polled in `wait_for_key()`'s 500-tick idle window (Test 2, no key → timeout → "PASS: no key arrived during bounded idle check." + `return 0`).  Run with NO keypost (idle branches deterministic, both print ONLY fixed text → fully toolchain-stable golden); **ZERO new code** — §6w's four keyboard aliases already staged `keyboard_hit`/`keyboard_getc_nonblock`, so just one battery entry + one golden.  SMALL (59,271 B); 30-s budget; bare-metal ONLY; FIRST-RUN PASS; no emit audit, no MP byte-compare.  **§6y (2026-06-13) gated the UNMODIFIED upstream `pic_test` bare-metal through bm_testhost — battery 37/37 → 38/38** (test-dos UNCHANGED 296/296, bare-metal-only like §6q/§6u/§6v/§6w): the **first gate of the `pic_enable_irq()`/`pic_disable_irq()` IRQ-mask API**.  Test 1 (`test_pic_mask`) reads the runtime IMR (0xBB = IR2 timer + IR6 keyboard enabled, the deterministic `bm_pic_init` state), disables/enables IR5 (an unused expansion bit, never disturbing the live timer/keyboard IRQs) asserting exact 0xBB/0x9B transitions then restores; Test 2 (`test_pic_with_timer`) waits ~100 ticks under the live timer ISR and asserts ticks advanced; the EOI test is implicit (continuous ticks ⇒ EOI works).  Two changes, build-glue only: (1) `bm_shim.c` gained four PIC aliases (`pic_get_mask`/`pic_set_mask` → `bm_pic_get_mask`/`set_mask`, `pic_enable_irq`/`pic_disable_irq` → `bm_pic_unmask`/`mask` — bm_pic.c is already linked into every bm_stdio build, the testhost preamble calls `bm_pic_init`), and (2) a new `minic/dos/newlibc/interrupts.h`, a minic-dialect port of `drivers/interrupts.h` (the upstream header is a GRATUITOUS include here — pic_test uses no symbol from it — but its `static inline get_interrupt_vector` carries ia16-gcc extended asm that minic emits dead + verbatim, which nasm rejects; the port reimplements it as a plain far-pointer read and no-ops `SAVE_ES`/`RESTORE_ES`, the §6e/§6i ES-drop reasoning — searched before $NL/drivers in the bare-metal include path, the bm_interrupts.h/bm_sasi.h porting pattern).  The mask-test values are fully deterministic; the timer test's 3 tick lines are timing-derived (run-stable, byte-identical across two MAME runs, WILL shift on a bm_tty/printf codegen change → re-capture then; PASS verdicts robust, §6v lesson).  This is newlibc bare-metal support glue (NOT compiler/qbe/emit/minic; MP does not link bm_shim.c/interrupts.h) → no emit audit, no MP byte-compare.  SMALL (60,121 B code); ~60 lines + a 1 s timer wait → 90-s budget; bare-metal ONLY (no live 8253/8259 on DOS).  FIRST-RUN PASS, verified `tools/test-newlibc.sh pic_test` → [ok]; additive changes confirmed non-disturbing by re-running `driver_test`/`keyboard_nonblock_test`/`stdin_test` → all [ok].  **See ROADMAP.md → Phase 6** for the plan (newlib proper out of near-term scope).  Next: with the keyboard family (raw-event §6w, nonblock-cooked §6x, cooked line/char §6n/§6o/§6t) and the PIC mask API (§6y) all gated, the remaining ungated phase-3 tests are `serial_loopback_test` (needs NEW harness plumbing — an rs232a TX→RX loopback attach, distinct from the §6e `V9K_SERIAL_IN` rs232b null_modem — plus its own RX-timing determinism) and `interrupt_test` (stays SKIPPED — §6v's `[90,110]` FAIL-window + raw iteration-count brittleness); the display-only/`hlt`-loop tests (memory/segment/simple_screen/font/font_ram/font_layout/minimal_irq) are NOT bm_testhost-shaped and already covered by the hand-mirrored `bm_*` ports; the newlibc-under-far-DATA-models (compact/large) stdio story when a far-DATA consumer appears; or pick from the carried open tracks.

---

## 📍 Status & history pointers

- **[ROADMAP.md](./ROADMAP.md)** — current component status, Phase 5 close-out, the Phase 6 (newlibc) plan.  *(Updated 2026-06-11.)*
- **[NEXT_SESSION.md](./NEXT_SESSION.md)** — per-session engineering notes, newest first (§5c at top).  The detailed history of every bug and fix lives here and in git, not in this file.
- **[MICROPYTHON_PORT.md](./MICROPYTHON_PORT.md)** — the MP port reference.
- **[I8086_TARGET.md](./I8086_TARGET.md)** / **[i8086/README.md](./i8086/README.md)** — backend reference.
- Memory files (auto-loaded) carry the distilled bug-class lessons ([[…]] links).

## Open tracks (pick by appetite; none urgent)

1. huge `_qbe_huge_add` ≥0x8000 variable-index gap (§4i).
2. `jmp_buf bufs[6]` cross-frame longjmp (§4v) — unreduced; reduce first.
3. ~~minic static-init float const-expr folding~~ — CLOSED/CORRECTED (§6z): folding was never broken; the real bug was missing `CONST`/`vol_qual` `TFLOAT`/`TDOUBLE` grammar productions (`const float`/`volatile double` etc. were parse errors).  Fixed + gated (`const_float_init_probe`); also unblocks MICROPY_PY_MATH_CONSTANTS (MP parked, so not enabled).
4. ~~Small-model setjmp/longjmp (near env)~~ — CLOSED (§7a): `NEAR_SETJMP_EXE` in `libstub_to_exe.py` (near `_setjmp`/`_longjmp`, 6-word jmp_buf, no CS word, near `ret`); gated small (`setjmp_probe` + `setjmp_clobber_probe`), test-dos 298→300, MP body 731,088 identical.
5. ~~Multi-decl items skip block_scope_decl (loud "double definition")~~ — CLOSED (§7b): a MULTI-declarator block-scope local (`T a, b;`, the first item included) shadowing a global/function/enum/different-typed outer local died "double definition" because `emit_local_multi_decl`/`_full` (+ the `int a=1,b=2;` rule's tail loop) called `varadd` directly, bypassing the §6a/§1k rename.  Fix routes every storage declarator through `block_scope_decl`; gated `multi_decl_shadow_probe` small+medium, test-dos 300→302, MP body 731,088 identical.
6. Kw spill-slot sharing — frame-size lever, no consumer pain.
7. ~~Stmt-context array-first multi-decl `int arr[3], *counter;` does not parse~~ — CLOSED (§7c): the stmt-context multi-decl production was only `type IDENT ',' ext_decllist` (bare leading IDENT); an array-decorated first declarator MID-BLOCK had no production (the dcls/function-top form already parsed).  Fix adds `type IDENT '[' expr ']' ',' ext_decllist ';'` mirroring the dcls array-first rule (kr_array_node + emit_local_multi_decl_full), deferring the init chain as an Expr stmt; conflicts unchanged.  Gated `arrayfirst_multidecl_probe` small+medium, test-dos 302→304, MP body 731,088 identical.

## House rules (process invariants that cost real time to relearn)

- **Gate before trust:** every fixed bug gets a reduced `minic/dos/examples/*_probe.c` wired into `tools/test-dos.sh` (bug-loud — verify the probe fails on the unfixed code) BEFORE the behavior is relied on.
- **minic staleness trap:** top-level make skips minic.y rebuilds.  Always `rm -f minic/minic && touch minic/minic.y && make minic/minic` and verify the .ssa changed before trusting a gate run.  See [[minic-make-staleness]].
- **After ANY i8086/emit.c change:** run `tools/run-emit-audit.sh` (the §4y standing tool — exact-liveness CHK markers + symbolic checker; 369 files / 117k regions / 0 violations is the baseline; ISR ret regions are tagged `live=isr` and skipped; the corpus CACHES probe asm — rm a probe's `build/chk-corpus` entries after changing its codegen).
- **After ANY toolchain change:** rebuild MP compact (`tools/build-micropython.sh --model=compact`, needs a far-data model since MP_SPLIT_STACK=1) and byte-compare the body.  Byte-identical ⇒ no Victor run.
- **Victor-bound probes MUST print per-phase progress** — a 5 MHz 8088 makes silence unreadable (a 700 s "hang" was just slowness).
- **Victor harness is deterministic** — empty serial output means a host-invocation mistake or a real hang, never "MAME flakiness".  Run `tools/run-victor-sasi.sh` foreground.
- **DOSBox fast loop for MP:** `MP_HEAP_SIZE=8192 MP_HEAP2_SIZE=12288 MP_STACK_SIZE=16384` → ~600 KB image that loads in DOSBox; PROG.PY beside the exe.
- **miniyacc gotchas:** no standalone `/* … */` comment between a `;` and the next production head; action-body comments must avoid `'` `/` `[` `]`.
- **Grammar conflict baseline:** 115 shift/reduce, 0 reduce/reduce.  Any change to that count needs justification.
- **Commit at green-gate milestones** to master without asking; stage only the session's files; no push/PR unless asked.

## Build & test quick reference

```sh
make check                      # QBE SSA tests
tools/test-dos.sh               # the 274-entry per-model runtime gate (DOSBox)
tools/build-example.sh --model=<tiny|small|medium|compact|large|huge> <file.c>
tools/build-stevie.sh --exe     # stevie editor
tools/build-micropython.sh --model=compact   # the MP regression corpus
tools/run-victor-sasi.sh <exe>  # real-hardware-equivalent MAME victor9k run
tools/build-newlibc-baremetal.sh hello_bm    # bare-metal raw binary @ 0x3000
tools/run-victor-baremetal.sh <bin>          # MAME Lua-loader bare-metal run
tools/test-victor.sh            # slow on-target gate (MAME; incl. bare-metal hello)
tools/run-emit-audit.sh         # emit-bracket liveness audit (after emit.c changes)
```

---

## Repository Information

**Repository:** https://github.com/pauldevine/qbe  (branch: master; PRs #23 upstream rebase, #24 §3p–§4y, #25 §5c, #26 §6a–§6b, #27 §6c–§6h)
**Upstream:** `upstream` remote → c9x.me/qbe.git — check upstream first for generic spill/SSA/regalloc bugs.  In sync through `e786f06` (2026-06-11).
**Related trees:** `~/projects/newlibc` (Phase 6 target), `~/projects/micropython` (dos8086 port), `/Applications/dosbox.app`, MAME victor9k.

This project is developed by Paul Devine with assistance from Claude (Anthropic).
