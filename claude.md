# Claude Session Status: QBE C11/C17 8086 Compiler

**Project:** C11/C17 + GNU-extensions C Compiler for 8086 DOS using QBE Backend
**Standard:** C11 feature set (`_Static_assert`, `_Generic`, `_Alignof`/`_Alignas`, compound literals, designated initializers, anonymous struct/union) + GNU extensions (`__attribute__`, inline `__asm__`, `__far` pointers).  Equivalently **C17-level** since C17 added no new language features over C11.  **No C23 language features.**  C only — no C++.
**Real target hardware:** Victor 9000 / Sirius 1 (~896 KB RAM) — NOT the IBM-PC 640 KB ceiling.  DOSBox is the fast loop for images that fit; MAME victor9k + SASI disk for the real thing.
**Last Updated:** 2026-06-17 (§8m)

---

## Where the project is (one paragraph)

The original compiler goal is **complete**, and so is the MicroPython hardening campaign that followed it.  All **six memory models work** (tiny .COM / small / medium / compact / large / huge — small was the last broken one, fixed 2026-06-11).  The per-model runtime gate is **289/289** (`tools/test-dos.sh`).  MicroPython (108 TUs) runs on a real Victor 9000: full language surface, FLOAT, the `math` module, a 114 KB split GC heap, an interactive REPL — all Victor-verified byte-exact vs host python3.  MicroPython's remaining feature tracks are **PARKED**; its role now is a **regression corpus** (rebuild compact, byte-compare the body after toolchain changes; Victor re-runs only when bytes move).  **As of §8d (2026-06-16) the corpus is the libstub-FREE build (`tools/build-micropython.sh` default; compact default-heap body = 689,760, was 731,088 under libstub); `--libstub` is the opt-out equivalence anchor (body 731,088).**  Upstream QBE is in sync through `e786f06`.  Stevie ships as a 146 KB medium-model .EXE, interactively verified on the Victor.

## 🚧 Next frontier: newlibc for the Victor 9000 (Phase 6, designated 2026-06-11)

Compile **`~/projects/newlibc`** — a much-progressed Victor 9000 C library + driver suite (bare-metal newlib port: crt0, libgloss, display/keyboard/timer/SASI drivers, VFS + read-only FAT, ~40 MAME-driven tests; phase 1 DOS drivers built with OpenWatcom, phase 3 bare-metal built with ia16-elf-gcc) — with **this** toolchain, and adopt its test suite as a standing robustness harness.  End state: newlibc replaces libstub as the real libc.  **Step 1 (triage sweep) DONE 2026-06-11 (§6a)**: 46/66 phase-3 TUs compile under small AND medium after seven minic dialect fixes.  **Step 2 (DOS-hosted portable subset) DONE 2026-06-11 (§6b)**: ELEVEN newlibc tests (snprintf, six FAT/VFS, ramfs, stdio_route, bss, terminal_meta) run DOS-hosted through the full newlibc stack and are standing gate entries (`tools/build-newlibc-test.sh`, `minic/dos/newlibc/`, `libstub_to_exe.py --no-stdio`); two toolchain bugs fixed en route (static file-scope data linkage; decimal `UL` literal typing).  **Step 3 (bare metal) DONE 2026-06-11 (§6c)**: the toolchain's first bare-metal program runs on the Victor 9000 under MAME — `omf_link.py --raw-binary --load-addr 0x3000` (link-time-absolute selectors + a synthesized register-setup stub at the image head), minic-built crt0 + polled NEC-7201 serial console (`minic/dos/newlibc/bm_*.c`, pure volatile-far MMIO, no inline asm), `hello_bm` PASS over serial (`tools/build-newlibc-baremetal.sh` / `tools/run-victor-baremetal.sh`; gated in `test_omf_link.sh` + `tools/test-victor.sh`).  **Step 4a (ISR ABI) DONE 2026-06-11 (§6d)**: `__attribute__((interrupt))` is a real compiler feature — QBE `interrupt` function linkage → the i8086 backend emits the ES-safe ISR prologue/epilogue itself (static CS-local ES save, all-register save, DS/ES=DGROUP via a link-time `dw DGROUP` word, iret); gated by `isr_probe` (small+medium) and by `timer_bm` taking LIVE 8253-ch2/IR2 timer interrupts through a minic-built ISR on MAME victor9k (victor pipeline 4/4; `bm_pic.c` full 8259A re-init is MANDATORY before any bare-metal `sti` — the boot ROM leaves IRQs unmasked on stale vectors).  **Step 4b (driver suite + battery) DONE 2026-06-12 (§6e)**: display (6845 + font RAM + glyph-pointer VRAM), keyboard (VIA-CS2 state machine, interrupt-driven on IR6), and a REAL serial-RX ISR (7201 channel B, IR1, drain-to-empty for the edge-triggered PIC) all run bare-metal through compiler-emitted ISRs — three new MAME-verified tests, each PASS on the first run, zero compiler changes; `tools/test-newlibc.sh` is the standing bare-metal battery (5/5: hello, timer, display, keyboard, serial), with harness keystroke/serial injection (`V9K_KEYPOST` via natkeyboard, `V9K_SERIAL_IN` via a mid-run-attached rs232b null_modem).  **The step-4 extended-asm question is ANSWERED: not needed** — every remaining driver inline-asm site was ia16-gcc ES damage control (the ISR ABI owns ES) or a pushf/cli flags-save the single-producer ring design obsoletes.  **Step 4c (battery sweep) DONE 2026-06-12 (§6f)**: the four "near-free" phase-3 tests ported and standing — `memory_bm` (font/screen RAM write-readback), `crtc_bm` (raw-VRAM pattern + R14/R15 cursor readback), `pic_bm` (IMR get/set + gating proof with the timer live; new `bm_pic_get_mask`/`set_mask`), `interrupt_bm` (the cross-driver stress: display init + 60 scrolling lines under the live timer ISR; needs a 120-emulated-second budget) — battery **9/9**, every port first-run PASS, zero compiler changes again.  **Step 4d (cooked console) DONE 2026-06-12 (§6g)**: `bm_tty.c/h` — the minic port of newlibc's `console_dev_read`/`console_dev_write` pair (output mirrored to display + serial; cooked keyboard input with Backspace/DEL rubout, CR→LF, blocking line reads) — the stdio seam for retiring libstub's INT 21h stdio; the `tty_bm` test types `vx\b9k\nz` through MAME's natural keyboard (a REAL Backspace keystroke through the full ISR path) and verifies the edited line in the buffer AND in VRAM — first-run PASS, zero compiler changes (third driver session running on the §6d ISR ABI); harness `V9K_KEYPOST` is now byte-safe (every byte → Lua `\ddd`, so control chars type real Victor keys: `\b`=Backspace, `\n`=Return), battery **10/10**.  **Step 4e (stdio over bm_tty) DONE 2026-06-12 (§6h)**: printf()/fgets() run on the bare machine through the REAL newlibc stack — the seam is **newlibc's VFS `/dev/console` routing moved bare-metal** (not a libstub-level swap): `bm_shim.c` is dos_shim.c's bare-metal parallel (console_dev_* → bm_tty, timer → bm_timer, display → bm_display, POSIX aliases + FILE layer), `bm_stdio.h` is the build probe pulling the full portable-subset TU set (printf/scanf wrappers, syscalls, vfs, fat, block — zero `#ifdef DOS` anywhere), and the `stdio_bm` battery test proves the whole path (printf format sweep, write(1), /dev/null fprintf, ramfs fread, isatty, `fgets(stdin)` returning `"v9k\n"` from a keypost with a REAL Backspace edit, getchar, times()) — FIRST-RUN PASS, zero compiler changes, battery **11/11**; upstream newlibc moved again (FAT WRITE + SASI WRITE(6)) with NO golden impact (the write support is a runtime-installed dispatch table we don't link).  **Step 4f (bare-metal disk I/O) DONE 2026-06-12 (§6i)**: the minic-built SASI/Xebec driver (`bm_sasi.c/h`, name-for-name port of newlibc's `drivers/sasi.c` — both upstream inline-asm constructs DROPPED, not translated: SAVE_ES was ia16-gcc damage control and pushf/cli is unnecessary under a REQ-driven handshake, proven by running every transfer with timer+keyboard ISRs live) reads and writes real sectors on MAME's `-scsi:0` hard disk, and the UNMODIFIED newlibc block/FAT/VFS stack mounts Victor virtual volume 0 from them (first minic coverage of `vfs_mount_victor_fat`'s disk-label path) — the `sasi_bm` battery test (geometry, LBA-0 label byte-exact vs host xxd, checksum repeat-read, stat/open/fgets of CONFIG.SYS, WRITE(6) round-trip @ LBA 59057 on the harness's scratch copy) was a FIRST-RUN PASS with zero compiler changes (fifth straight driver session on the §6d ISR ABI); harness grew `V9K_HARD_DISK` (scratch-copy attach) and a per-test disk field; battery **12/12**.  **Step 4g (DOS-hosted tests re-run bare-metal) DONE 2026-06-12 (§6j)**: the ten portable DOS-hosted newlibc tests (snprintf, stdio_route, the six FAT/VFS, terminal_meta, ramfs) now run UNMODIFIED on the bare machine through bm_stdio — `build-newlibc-baremetal.sh` grew a **test-host mode**: a source resolving into newlibc's `tests/` directory gets the same `-Dmain=newlibc_test_main` rename the DOS gate uses, with new `minic/dos/newlibc/bm_testhost.c` (driver bring-up in the §6d interrupt-window order → `vfs_init()` → call the test → result line + `__V9END__`) as `main()` and the full bm_stdio TU set linked; each test's bare-metal serial output is **line-identical** to its DOS-hosted golden between the testhost preamble and result line (the ten new goldens are `minic/dos/tests/<name>.golden.txt`).  All ten were FIRST-RUN PASS, zero compiler changes (sixth straight session on the §6d ISR ABI); battery **22/22** (`test-newlibc.sh` grew the ten entries; ramfs_test needs a 300-emulated-second budget for its 103 output lines per the §6f scroll lesson).  `bss_test` stays DOS-hosted only (display-only output + a hlt idle loop; `memory_bm` already covers bare-metal RAM).  Upstream newlibc moved again (mkdir/rmdir/rename + the FAT-write commits now merged through PR #19) with NO golden impact — `test-dos` still 289/289, the write support remains a runtime-installed dispatch table we never link.  **Step 4h (FAT WRITE, DOS-hosted half) DONE 2026-06-13 (§6k)**: the UNMODIFIED upstream `fat_write_test.c` now PASSES through the full newlibc FAT WRITE layer (`vfs_mount_fat_rw` + create/multi-cluster-write/read-back/append/truncate/unlink/mkdir/rmdir/rename over a RAM disk) — and doing it required the **first MEDIUM-model DOS-hosted newlibc port**, because `fat_write.c` (~18 KB code) over the FAT/VFS/stdio stack overflows the small model's single-`_TEXT` 64 KB code ceiling in EVERY host (measured: bare-metal 81 KB, DOS upstream 77 KB, a stripped minimal DOS test still 68.6 KB; the floor `fat.c`+`fat_write.c`+`vfs.c` is irreducible).  Medium is far CODE (code splits across per-TU `<=64 KB` CS segments) + near DATA (one DGROUP; minic does NOT `far_stdlib`-mangle under `NEAR_DATA()`, which includes medium, so newlibc stdio links by real name under `--no-stdio`).  Two over-conservative guards lumping medium with far-DATA were relaxed (`build-newlibc-test.sh` small→small+medium + `--stack-size`; `libstub_to_exe.py --no-stdio` `near_code`→new `near_data_model`).  **One real compiler/toolchain bug fixed**: `asm_to_omf.py` split a relocatable `.long _sym` initializer into `dw off + dw seg` only for far-DATA models; in medium every `.long _sym` is a 4-byte far CODE pointer (function-pointer field in a static device-ops table — `console_device.write`) but fell through to `dd` (offset only, **segment word 0**), so the indirect far CALL through it jumped to seg 0 → hang (`split_sym_long = far_data or model == 'medium'`, scoped to medium to keep the compact/large/huge corpus byte-identical).  `dos_shim` gained the write side of its FILE layer (`fwrite`; `fopen` `"w"/"a"/"+"` mode→`O_CREAT/O_TRUNC/O_APPEND/O_RDWR`).  Gated as `newlibc medium (fat_write_test)` in `test-dos.sh`; **test-dos 290/290**, all 11 small newlibc tests still golden-identical, **MP compact body 731,088 bytes byte-identical** (compact provably unaffected), stevie (medium) still builds — no `emit.c` change.  **Step 4h (FAT WRITE, bare-metal half) DONE 2026-06-13 (§6l)**: the bare-metal `fatwrite_bm.c` now PASSES on the real `-scsi:0` SASI disk through the full newlibc FAT WRITE layer — the toolchain's **first MEDIUM-model BARE-METAL program** (88 KB code, over the small single-`_TEXT` 64 KB ceiling, confirming medium multi-CS is mandatory on bare metal too).  **FIRST-RUN PASS, ZERO compiler/toolchain changes** — everything medium needs was already in place from §6k (the `asm_to_omf` far CODE-pointer static-init fix, `NEAR_DATA()` covering medium, the `libstub_to_exe` `near_data_model` guard); omf_link's `--raw-binary` path is natively medium (multi-CS, resolves the entry CS:IP for the stub unchanged), `bm_crt0.c` far-calls `main` automatically under `minic -m medium`, and the §6d ISR ABI works unchanged (the timer ISR's far function pointer carries its real CS; `iret` is model-independent).  The only changes were build/harness plumbing: `build-newlibc-baremetal.sh` gained `--model=small|medium`, and `test-newlibc.sh` an optional sixth `:<model>` field (`fatwrite_bm:240:::hd:medium`, the §6i `V9K_HARD_DISK` scratch-copy path).  Phase 8's multi-cluster SASI write on the 5 MHz 8088 dominates the budget (240 emulated seconds — 90 truncated mid-write, the §6f slowness lesson, NOT a hang).  No toolchain change → no emit audit, no MP byte-compare.  Step 4h is now COMPLETE on both halves.  **§6m (2026-06-13) added a SECOND medium gate, both halves, zero toolchain changes**: the UNMODIFIED upstream `fat_write_unit_test.c` (FAT16 entry write/read + both-FAT mirroring + cluster-chain alloc/free + create/write/truncate/unlink/mkdir/rename + ENOSPC, and FAT12 entries straddling a FAT sector boundary in both parities — exercising the `fat_write.c` primitives DIRECTLY on hand-built RAM volumes, so no SASI) now PASSES DOS-hosted (`test-dos` **291/291**) AND bare-metal through bm_stdio test-host mode (`fat_write_unit_test`, RAM-disk only → no `-scsi:0`; battery **24/24**) — FIRST-RUN PASS on both, same medium support from §6k.  The only changes were build/harness plumbing: `build-newlibc-baremetal.sh` gained `--stack-size` (mirroring the DOS build; the test's hand-built RAM-volume `media[]` arrays + the full bm_stdio driver set push data+bss to 60,688 B, so the default 8 KB stack overflows the 64 KB DGROUP — it runs at 4096), and `test-newlibc.sh` grew an optional seventh `:<stack>` entry field (the entry-parser was rewritten to clean `IFS=:` field-splitting, verified equivalent for all existing entries).  **§6n (2026-06-13) gated the two keyboard-input tests DOS-hosted, zero toolchain changes**: `stdin_test` (getchar/fgets) and `scanf_test` (scanf) — parked since §6k for want of a harness input channel — now run small-model through a **DOS stdin redirect** (`PROG < IN.TXT`; on DOS, INT 21h AH=3Fh on handle 0 reads the file raw, no echo, so the run is deterministic).  Plumbed backward-compatibly through three layers: `run-dos-exe.sh` `$DOS_STDIN` env var; `run-dos-batch.sh` optional 3rd TAB manifest field (host stdin file → staged `Tnnnn.IN` → `Tnnnn.EXE < Tnnnn.IN > Tnnnn.TXT`; 2-field legacy lines parse with an empty 3rd field, command byte-identical); `test-dos.sh` `stage_runtime_case` optional 4th arg threaded into that field.  Fixtures + redirect-captured goldens in `minic/dos/tests/newlibc_{stdin,scanf}_test.{stdin,golden}.txt`; **test-dos 293/293**, no compiler source touched → no emit audit, no MP byte-compare.  **§6o (2026-06-13) drove those SAME two keyboard-input tests BARE-METAL, zero toolchain changes**: `stdin_test` and `scanf_test` now run small-model through the cooked `bm_tty` console (interrupt-driven keyboard on IR6) as standing battery entries — `getchar`/`fgets`/`scanf` reach `_read(0,…)` → `console_dev_read` → `bm_tty_read` (`getchar` reads ONE keystroke, count=1, no Enter needed; the line readers stop at the echoed Return), all fed by a single `V9K_KEYPOST` natkeyboard burst into the IR6 ring so timing is irrelevant — except a throwaway char AFTER the final `\n` is REQUIRED to commit that Return into the ring before the program blocks on it (the §6h `stdio_bm` flush lesson; entries `stdin_test:35:Ahello\nz` and `scanf_test:35:victor 42\nz`).  Both were FIRST-RUN PASS on MAME (getchar='A' / fgets="hello\n"; %15s="victor" / %d=42); the new goldens `minic/dos/tests/{stdin,scanf}_test.golden.txt` differ from §6n's DOS-redirect goldens because the cooked console ECHOES the typed input, and they build small-model (no `fat_write.c` bulk).  Only `test-newlibc.sh` + the two goldens changed → no emit audit, no MP byte-compare; **battery 26/26**.  **§6p (2026-06-13) ran three UNMODIFIED upstream SASI-backed FAT tests bare-metal on the REAL `-scsi:0` disk, zero compiler/toolchain changes — battery 29/29**: where §6l's `fatwrite_bm` was a hand-mirrored minic TU, this runs the upstream tests THEMSELVES through bm_testhost + the full bm_stdio/VFS/FAT stack — `sasi_fat_smoke_test` (read CONFIG.SYS, small, 64,771 B code), `sasi_fat_dir_test` (root + subdir iteration via `vfs_mount_victor_fat`; adds `dirent.c` → 66,435 B overflows small's 64 KB `_TEXT` → MEDIUM, a read-path re-confirmation of the §6k/§6l ceiling lesson), and the headline `sasi_fat_write_test` (create/write 2000 B / read-back / append / unlink on the real disk via `vfs_mount_victor_fat_rw` + SASI WRITE(6), CONFIG.SYS intact before+after; MEDIUM, 240-s budget for the Phase-8 multi-cluster write).  All three FIRST-RUN PASS against a scratch copy of `victor_30mb.img` (`hd` field → `V9K_HARD_DISK`).  These run bare-metal ONLY (the DOS host has no raw SASI), so their goldens are captured from the bare-metal run, not DOS-diffed.  The lone change was widening `build-newlibc-baremetal.sh`'s SASI TU probe from `bm_sasi\.h` to `sasi\.h` (the upstream tests `#include "sasi.h"`; `bm_sasi.h` is a byte-for-byte port and `bm_sasi.c` the only SASI impl we link) — no compiler/qbe/emit source touched.  Entries `sasi_fat_smoke_test:60:::hd`, `sasi_fat_dir_test:90:::hd:medium`, `sasi_fat_write_test:240:::hd:medium`; verified `[ok] [ok] [ok]` through the battery harness.  **§6q (2026-06-13) ran the UNMODIFIED upstream `sasi_sector_test` raw-block probe bare-metal on the real `-scsi:0` disk, zero compiler/toolchain/build-script changes — battery 30/30**: the read-only block-layer counterpart to §6i's hand-mirrored `sasi_bm` (and to the §6p FAT family — runs the upstream test ITSELF) — registers the SASI block device, reads LBA 0 twice with a `block_cache_invalidate` between, checks the two checksums match (0x8DDD), dumps the first 32 bytes (the `tandon_703_mame` label, no boot-sector signature) and the SASI bus/diagnostics (geometry 59058×512).  FIRST-RUN PASS against a scratch copy of `victor_30mb.img`.  **Model lesson re-confirmed a third time:** "read-only" ≠ small-model — 65,577 B code in small is 41 B over the 65,536 `_TEXT` ceiling (wraps and hangs, the §6p `sasi_fat_dir_test` symptom), so MEDIUM (70,944 B multi-CS); reads LBA 0 only → 60-s budget enough.  Needed NOTHING new: §6p already widened the SASI TU probe to `sasi\.h` (the test `#include`s both `block.h` and `sasi.h`), so the lone diff is one battery entry (`sasi_sector_test:60:::hd:medium`) + one bare-metal-captured golden → no emit audit, no MP byte-compare.  **§6r (2026-06-13) gated the UNMODIFIED upstream `fat_victor_label_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 294/294, battery 31/31**: unlike the §6i/§6p SASI tests (raw `-scsi:0`, bare-metal-only) this is RAM-disk style — it hand-builds a Victor drive-label + volume-label + FAT12 in a `media[]` array via `block_register_ramdisk`, so it ALSO runs DOS-hosted (added to `NEWLIBC_TESTS` in `test-dos.sh`) and its bare-metal serial output is line-identical to the DOS golden between the bm_testhost preamble and the result line (the §6j RAM-disk pattern).  It is the **first deterministic golden for the Victor drive-label → volume-label → relative-data-start parse path** (`fat_mount_victor` / `vfs_mount_victor_fat`): the other RAM-disk FAT tests use standard BPB mounts, and this path was previously covered ONLY bare-metal-on-real-SASI (§6i, §6p).  Builds SMALL in both hosts (DOS 52,003 B; bare-metal 60,697 B, under the 64 KB `_TEXT` ceiling — no medium, contrast the §6p/§6q read-path tests).  FIRST-RUN PASS on MAME; battery entry `fat_victor_label_test:60:::`, goldens `minic/dos/tests/{newlibc_,}fat_victor_label_test.golden.txt`; the DOS golden verified byte-exact through the run-dos-batch path.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6s (2026-06-13) gated the UNMODIFIED upstream `block_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 295/295, battery 32/32**: the block-device layer tested DIRECTLY one level below FAT — `block_register_ramdisk` + `block_init`, single/multi-sector read, write-through cache + `block_cache_invalidate` refresh, `block_get_info`/`block_status`/`block_cache_flush`, and the EINVAL/ENODEV/EROFS error paths, all over RAM-backed media (no `-scsi:0`).  Like §6r's `fat_victor_label_test` it is RAM-disk style, so it runs BOTH DOS-hosted (added to `NEWLIBC_TESTS` in `test-dos.sh`, golden byte-exact through run-dos-batch at 295/295) AND bare-metal (battery entry `block_test:60:::`, body line-identical to the DOS golden between the bm_testhost preamble and result line).  This is the **first deterministic golden for the block layer in isolation** — the FAT tests reach it transitively but never assert its cache/error semantics.  Builds SMALL in both hosts (DOS 51,811 B; bare-metal 60,505 B, under the 64 KB `_TEXT` ceiling — a RAM-disk block-layer TU set is leaner than the §6p/§6q SASI read-path tests that pulled dirent.c over the ceiling).  FIRST-RUN PASS on MAME and through the DOS gate; goldens `minic/dos/tests/{newlibc_,}block_test.golden.txt`.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6t (2026-06-13) gated the UNMODIFIED upstream `read_test` BOTH DOS-hosted AND bare-metal, zero compiler/toolchain/build-script changes — test-dos 296/296, battery 33/33**: the raw `read(0,…)` keyboard-input layer, the third member of the §6n/§6o keyboard family after `stdin_test` (getchar/fgets) and `scanf_test` (scanf).  It exercises `read(STDIN_FILENO,&ch,1)` (one byte) and `read(STDIN_FILENO,line,39)` (a cooked line) DIRECTLY — asserting the syscall byte count, that the buffer stops at the newline, and that it carries no `\b` byte — coverage the wrapper tests reach only transitively through the same `_read(0,…)` path.  DOS-hosted via the §6n stdin redirect (`< IN.TXT`, AH=3Fh raw on handle 0, no echo): fixture `Ahello\n` (no Backspace — the raw redirect performs no rubout), added to the §6n loop in `test-dos.sh` (`stdin_test scanf_test read_test`), golden verified byte-exact through run-dos-batch.  Bare-metal via the §6o cooked `bm_tty` console: entry `read_test:35:Avx\b9k\nz::` — `read(0,&ch,1)`='A', `read(0,line,39)` reads `vx\b9k` cooked → a REAL Backspace rubs out the `x` → "v9k\n" (4 bytes), trailing `z` flushes the final Return into the IR6 ring; golden ECHOES the input (vs the no-echo DOS golden), same §6n/§6o divergence as stdin/scanf.  Builds SMALL both hosts (DOS 51,117 B; bare-metal 59,811 B — portable stdio, no fat_write.c bulk).  FIRST-RUN PASS on MAME and through the DOS gate; goldens `minic/dos/tests/{newlibc_,}read_test.golden.txt` + fixture `newlibc_read_test.stdin.txt`.  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6u (2026-06-13) gated the UNMODIFIED upstream `driver_test` bare-metal through bm_testhost, zero compiler/toolchain/build-script changes — battery 33/33 → 34/34** (test-dos UNCHANGED 296/296, a bare-metal-only gate like §6q): the **first DRIVER-layer upstream test** (§6j–§6t ran upstream tests over the portable stdio/vfs/fat/block surface; the drivers were covered only by the hand-mirrored `bm_*` ports).  It validates the Phase-1 hardware-fix story against the LIVE bm_timer/8259 — Test 1 measures a real 100 ms `timer_delay_ms` and asserts ~10 ticks (deterministic 10 in MAME: both `timer_get_ticks()` and `delay_ms`'s start read the same ISR `tick_counter` on consecutive instructions, no tick between), Test 2 asserts `timer_get_frequency()==100`, Test 4 reads the live 8259 IMR through the `PIC_GET_MASK()` MMIO macro (direct `volatile __far` read of E000:0001 → 0xBB, IR2 unmasked), all five tests print fixed text + PASS and `main` returns 0.  The driver surface resolves through `bm_shim.c` (`timer_*` → `bm_timer`, `timer_get_frequency` → `100UL`) + the `v9k_hw.h` PIC/timer MMIO macros — NOTHING new to link, so like §6q the only changes are one battery entry (`driver_test:90:::`) + one bare-metal-captured golden (`minic/dos/tests/driver_test.golden.txt`).  Bare-metal ONLY (the DOS host has no live 8253/8259 → the measured-delay and live-IMR lines have no DOS golden).  Builds SMALL (59,485 B code, under the 64 KB `_TEXT` ceiling); 71 output lines at the §6f display-scroll rate need a 90-emulated-second budget (60 s truncates mid-Test-5).  FIRST-RUN PASS on MAME; verified `tools/test-newlibc.sh driver_test` → [ok] (the harness diffs live serial against the golden, so [ok] IS the gate).  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6v (2026-06-13) gated the UNMODIFIED upstream `simple_interrupt_test` bare-metal through bm_testhost, zero compiler/toolchain/build-script changes — battery 34/34 → 35/35** (test-dos UNCHANGED 296/296, a bare-metal-only gate like §6q/§6u): the minimal CONTINUOUS-timer-interrupt test (33-line upstream TU: read a start tick count, then 5× `timer_delay_ms(1000)` each printing elapsed ticks, then an UNCONDITIONAL `PASS: Interrupts working!`).  Complements §6u's `driver_test` (which measured a SINGLE 100 ms delay): this proves 5 seconds of CONTINUOUS timer interrupts keep the ISR `tick_counter` incrementing monotonically under the full bm_stdio/bm_timer stack — the longest sustained-interrupt run in the battery.  Resolves entirely through `bm_shim.c` (`timer_get_ticks`/`timer_delay_ms` → `bm_timer_*`) — NOTHING new to link, so like §6q/§6u just one battery entry (`simple_interrupt_test:30:::`) + one bare-metal-captured golden.  **Chosen over the larger `interrupt_test`, which is UNSUITABLE**: its Test 1 reads `start_ticks` BEFORE four slow display-mirrored printfs, so accumulated scroll ticks push elapsed past its `[90,110]` window → it prints `FAIL` (wrong to gate), and its Test 3 embeds a raw busy-loop iteration count.  **Note on the golden's tick values** (`Start: 111`, elapsed `155/316/476/637/797`): MAME models the Victor channel-2 clock FASTER than the nominal 100 Hz (the upstream comment warns of this) and the display-mirrored printf between delays adds ~61 ticks, so elapsed grows ~161/iteration — these are DISPLAY-SCROLL-TIMING-derived, RUN-STABLE (cycle-deterministic, verified byte-identical across three runs) but they WILL SHIFT if a bm_tty/printf codegen change alters timing → re-capture then (the PASS verdict is unconditional, so a shift is a LOUD diff, never a silent wrong pass).  Bare-metal ONLY (no live 8253 on DOS); SMALL (58,723 B code); 30-s budget.  FIRST-RUN PASS on MAME, verified `tools/test-newlibc.sh simple_interrupt_test` → [ok].  No compiler/build-script source touched → no emit audit, no MP byte-compare.  **§6w (2026-06-13) gated the UNMODIFIED upstream `keyboard_raw_test` bare-metal through bm_testhost — battery 35/35 → 36/36** (test-dos UNCHANGED 296/296, bare-metal-only like §6q/§6u/§6v): the **first gate of the RAW (uncooked) keyboard event nonblock API** (the §6e `keyboard_bm` and the §6n/§6o/§6t `stdin/scanf/read` family all exercise the COOKED `bm_tty` path; this calls `keyboard_get_raw_event_nonblock()` directly against the interrupt-driven IR6 event ring beneath the cooking).  Run with **NO keypost** so the raw ring stays empty → after a 500-tick (5 s) idle window it takes the `count==0` branch and prints `PASS: no raw keyboard events arrived during idle check.` + `return 0`.  Driving with no keypost SIDESTEPS the keypost-vs-poll timing race the keyboard tests were parked on (no keys → no race; idle branch deterministic), and the idle branch prints **ONLY fixed text — no tick values** → the golden is fully toolchain-stable (strictly better than §6v's timing-derived ticks).  **One change, build-glue only**: `bm_shim.c` gained keyboard surface aliases (`keyboard_get_raw_event_nonblock`/`keyboard_hit`/`keyboard_getc`/`keyboard_getc_nonblock` → `bm_keyboard_*`, mirroring the file's existing `timer_*`/`display_*` surfaces) so the unmodified test links its unprefixed names — bm_keyboard.c is already linked into every bm_stdio build and `bm_tty_init()` already inits the IR6 ISR, so nothing NEW links, only wrapper symbols (the aliases also pre-stage `keyboard_nonblock_test`).  This is a newlibc bare-metal support TU (NOT compiler/qbe/emit/minic; MP does not link bm_shim.c) → **no emit audit, no MP byte-compare**.  Builds SMALL (59,223 B code); 30-s budget; bare-metal ONLY (no live IR6 ring / 8253 on DOS).  FIRST-RUN PASS, verified `tools/test-newlibc.sh keyboard_raw_test` → [ok]; additive aliases confirmed non-disturbing by re-running `stdin_test`/`stdio_bm`/`snprintf_test` → all [ok].  **§6x (2026-06-13) gated the UNMODIFIED upstream `keyboard_nonblock_test` bare-metal through bm_testhost — battery 36/36 → 37/37** (test-dos UNCHANGED 296/296, bare-metal-only): the nonblock-COOKED-byte pair one layer above §6w's raw-event API — `keyboard_getc_nonblock()` (Test 1, no key pending → `< 0` → "OK: no key was pending.") and `keyboard_hit()` polled in `wait_for_key()`'s 500-tick idle window (Test 2, no key → timeout → "PASS: no key arrived during bounded idle check." + `return 0`).  Run with NO keypost (idle branches deterministic, both print ONLY fixed text → fully toolchain-stable golden); **ZERO new code** — §6w's four keyboard aliases already staged `keyboard_hit`/`keyboard_getc_nonblock`, so just one battery entry + one golden.  SMALL (59,271 B); 30-s budget; bare-metal ONLY; FIRST-RUN PASS; no emit audit, no MP byte-compare.  **§6y (2026-06-13) gated the UNMODIFIED upstream `pic_test` bare-metal through bm_testhost — battery 37/37 → 38/38** (test-dos UNCHANGED 296/296, bare-metal-only like §6q/§6u/§6v/§6w): the **first gate of the `pic_enable_irq()`/`pic_disable_irq()` IRQ-mask API**.  Test 1 (`test_pic_mask`) reads the runtime IMR (0xBB = IR2 timer + IR6 keyboard enabled, the deterministic `bm_pic_init` state), disables/enables IR5 (an unused expansion bit, never disturbing the live timer/keyboard IRQs) asserting exact 0xBB/0x9B transitions then restores; Test 2 (`test_pic_with_timer`) waits ~100 ticks under the live timer ISR and asserts ticks advanced; the EOI test is implicit (continuous ticks ⇒ EOI works).  Two changes, build-glue only: (1) `bm_shim.c` gained four PIC aliases (`pic_get_mask`/`pic_set_mask` → `bm_pic_get_mask`/`set_mask`, `pic_enable_irq`/`pic_disable_irq` → `bm_pic_unmask`/`mask` — bm_pic.c is already linked into every bm_stdio build, the testhost preamble calls `bm_pic_init`), and (2) a new `minic/dos/newlibc/interrupts.h`, a minic-dialect port of `drivers/interrupts.h` (the upstream header is a GRATUITOUS include here — pic_test uses no symbol from it — but its `static inline get_interrupt_vector` carries ia16-gcc extended asm that minic emits dead + verbatim, which nasm rejects; the port reimplements it as a plain far-pointer read and no-ops `SAVE_ES`/`RESTORE_ES`, the §6e/§6i ES-drop reasoning — searched before $NL/drivers in the bare-metal include path, the bm_interrupts.h/bm_sasi.h porting pattern).  The mask-test values are fully deterministic; the timer test's 3 tick lines are timing-derived (run-stable, byte-identical across two MAME runs, WILL shift on a bm_tty/printf codegen change → re-capture then; PASS verdicts robust, §6v lesson).  This is newlibc bare-metal support glue (NOT compiler/qbe/emit/minic; MP does not link bm_shim.c/interrupts.h) → no emit audit, no MP byte-compare.  SMALL (60,121 B code); ~60 lines + a 1 s timer wait → 90-s budget; bare-metal ONLY (no live 8253/8259 on DOS).  FIRST-RUN PASS, verified `tools/test-newlibc.sh pic_test` → [ok]; additive changes confirmed non-disturbing by re-running `driver_test`/`keyboard_nonblock_test`/`stdin_test` → all [ok].  **See ROADMAP.md → Phase 6** for the plan (newlib proper out of near-term scope).  **§7i (2026-06-14) gated the UNMODIFIED upstream `serial_loopback_test` bare-metal through bm_testhost, zero compiler/qbe/emit/minic changes — battery 38/38 → 39/39** (test-dos UNCHANGED, bare-metal-only): the **first gate of the newlibc raw-serial console API** — `console_putc`/`console_getc_nonblock`/`console_rx_ready` on 7201 channel A (Test 1) and `/dev/tty` write/read (Test 2), both over a hardware TXD→RXD loopback.  Two NEW capabilities, both newlibc-support-glue only: (1) **channel-A polled RX** in `bm_console.c` (`bm_console_getc`/`_getc_nonblock`/`_rx_ready` + `bm_console_putc`; channel-A RX was already enabled by `bm_console_init`'s WR3=0xC1, this completes the polled path), aliased to the unprefixed `console_*` names in `bm_shim.c`, AND `tty_dev_*` re-routed there to match upstream `drivers/console.c` (where `/dev/tty` IS the raw serial debug console = `console_getc`/`console_putc`, DISTINCT from `/dev/console`'s cooked keyboard — our default bare-metal port had lazily folded the two onto the cooked `bm_tty`; serial_loopback_test is the first test to distinguish them); (2) **a MAME loopback harness mode** — channel A becomes `-rs232a loopback` (the test's TXD→RXD data path, which is NOT a bitbanger device), so the captured testhost debug console MOVES to channel B (`-rs232b null_modem -bitbanger`), and `bm_console.c` under `-DBM_SERIAL_LOOPBACK` routes `bm_putc` (the harness output: testhost preamble + `printf` result line) to a polled channel-B TX it programs (counter-1 baud + the WR sequence `bm_serial.c` uses, polled).  ALL the new behavior is `#ifdef BM_SERIAL_LOOPBACK`-gated (defined ONLY for serial_loopback_test via a per-test `EXTRA_CFLAGS` in `build-newlibc-baremetal.sh`) plus additive (`bm_console_*` RX fns + a `display_putc` alias, `--gc-sections`-stripped when unreferenced), so every other bm build is behavior-identical (re-verified `snprintf_test`/`stdin_test`/`stdio_bm`/`serial_bm` → all [ok]).  The test writes its own PASS/FAIL to the DISPLAY (VRAM, uncaptured); the golden is the testhost result line, so `test returned 0` proves both subtests round-tripped all 8 bytes (5 + 3) through the channel-A loopback (bug-loud: any non-loopback channel-A device both breaks the loopback AND loses the channel-B capture → the gate fails loudly, never a spurious `returned 0`; and without the channel-A RX code the build won't even link — `console_*` undefined).  Plumbing: `run-victor-baremetal.sh` `$V9K_SERIAL_LOOPBACK=1`, `test-newlibc.sh` an optional 8th `:<loopback>` entry field (`lb`), golden `minic/dos/tests/serial_loopback_test.golden.txt`.  SMALL (61,171 B `_TEXT`, under the 64 KB ceiling; DGROUP 54,810 B); FIRST-RUN PASS on MAME, deterministic across runs, 30-s budget.  This is newlibc bare-metal support glue (NOT compiler/qbe/emit/minic; MP does not link `bm_console.c`/`bm_shim.c`) → **no emit audit, no MP byte-compare**.  Next: with the keyboard family (raw-event §6w, nonblock-cooked §6x, cooked line/char §6n/§6o/§6t), the PIC mask API (§6y), and now the serial loopback / raw-serial console (§7i) all gated, the only remaining ungated phase-3 test is `interrupt_test` (stays SKIPPED — §6v's `[90,110]` FAIL-window + raw iteration-count brittleness); the display-only/`hlt`-loop tests (memory/segment/simple_screen/font/font_ram/font_layout/minimal_irq) are NOT bm_testhost-shaped and already covered by the hand-mirrored `bm_*` ports; the newlibc-under-far-DATA-models (compact/large) stdio story when a far-DATA consumer appears; or pick from the carried open tracks (Kw spill-slot sharing; the bounded aoa init/multi-declarator gap §7e).  **§7j (2026-06-14) closed the bounded aoa gap (§7e) for its parseable forms, frontend-only — battery/Victor untouched, test-dos 317→320, MP body 731,088 byte-identical**: the §7e single-declarator fix had left two declarator SHAPES still ignoring the array typedef inner dim `g_td_arraydim` — (1) **multi-declarator** `jmp_buf a[2], b[2];` (block-local), whose `'B'` sized-array path sized `count*sizeof(elem)` not `count*D*sizeof(elem)` and never set the aoa flag (so `b[i]` was a scalar value-as-pointer load and `setjmp(b[i])` ran through garbage), and (2) **brace-init 2-D tables** `row3_t t[2] = {{1,2,3},{4,5,6}};` (the ONLY way to write a 2-D constant table, since `int x[2][3]` is a hard parse error), where the four local brace-init rules sized `sizeof(elem)` and stored each item with one scalar `expr()` so a nested `{…}` row Abort-trap-6'd the compiler.  Fixed all in `minic.y` (frontend): the `emit_local_multi_decl_full` `'B'` branch now honors `aoa = g_td_arraydim` (size `count*D*sizeof(elem)` + `var_set_aoa_dim`), and a new `mk_aoa_array_init` helper flattens each `{…}` row into per-element stores `*(v + (r*dim+c))` built as RAW `'@'(+ V off)` nodes that BYPASS `mkidx` (so the linear index is not re-multiplied by D; the bare `'+'` Scale scales by `sizeof(elem)` via `v`'s `IDIR(elem)` decay), wired into all four brace-init rules (dcls/stmt × sized/unsized) under an `aoa>0` branch — every `aoa==0` path left textually unchanged so non-aoa codegen is byte-identical.  Scope bounded to the PARSEABLE forms (block-local multi-decl + block-local brace-init): the file-scope / static multi-decl array-first form (`static jmp_buf fa[2], fb[2];`) is a SEPARATE pre-existing grammar **parse-error** gap (not aoa sizing) and stays untouched, as does plain `jmp_buf a, b;` (array-typedef-instance) multi-decl — no parseable/realistic consumer to gate them bug-loud.  Gated `aoa_extended_probe` medium+compact+large (bug-loud: unfixed compiler ABORTS — scalar-load multi-decl + nested-brace abort); conflicts UNCHANGED (115 s/r — no new productions, only actions), `make check` green, MP body 731,088 byte-identical → no Victor run, frontend → no emit audit.  **§7l (2026-06-14) gated the UNMODIFIED upstream `font_ram_test` bare-metal through bm_testhost — battery 39/39 → 40/40** (test-dos UNCHANGED, bare-metal-only like §6q/§6u/§6v/§6w/§6y): the **first gate of the REAL font-loading path** — `display_init()` → `display_load_fonts()` copies all 8192 bytes of the native `victor_font[]` table to font RAM at `0000:0C00`, then `verify_font_ram()` reads it back and byte-compares against `victor_font[]` (3429 non-zero bytes), printing PASS/FAIL via `printf` (serial-capturable through bm_stdio).  **Unique coverage:** the hand-mirrored `memory_bm` (§6f) only does arbitrary-pattern write-readback of font RAM — it never exercises the real `display_load_fonts()` copy-vs-table correctness, which every text-mode program on the ROM-less Victor depends on.  **The §7i scoping note had lumped this with the "display-only/`hlt`-loop" tests as NOT bm_testhost-shaped, but that was over-conservative:** its result goes to `printf` (serial), and although it ends in `while(1) hlt` (never returns, so no `bm_testhost: test returned`/`__V9END__` line), `run-victor-baremetal.sh` captures `__V9BEGIN__`→end-of-budget and exits 0 whenever `__V9BEGIN__` is present (MAME always runs the full budget — no early-exit on `__V9END__`), so the golden simply ends at the test's own PASS line.  **One change, build-glue only:** `bm_shim.c` gained a `display_init` → `bm_display_init` alias (joining the existing `display_puts`/`putc`/`clear`/`set_cursor` display surface; `bm_display.c` + `bm_font_data.c` — which DEFINES `victor_font` — are already linked into every bm_stdio build, so nothing NEW links, only the wrapper symbol, `--gc-sections`-stripped when unreferenced).  The output is fully deterministic (no timer values), so the golden is toolchain-stable AND bug-loud: broken font loading prints `FAIL: font RAM mismatch count N` and diffs, and a missing `display_init` won't even link.  Bare-metal ONLY (the DOS host has no Victor font RAM).  SMALL (59,563 B code, under the 64 KB `_TEXT` ceiling); 30-s budget; FIRST-RUN PASS on MAME (verified `tools/test-newlibc.sh font_ram_test` → [ok]); additive alias confirmed non-disturbing by re-running `stdio_bm`/`driver_test`/`snprintf_test` → all [ok].  This is newlibc bare-metal support glue (NOT compiler/qbe/emit/minic; MP does not link `bm_shim.c`) → **no emit audit, no MP byte-compare**.  **§7m (2026-06-14) gated the UNMODIFIED upstream `font_test` bare-metal through bm_testhost — battery 40/40 → 41/41** (test-dos UNCHANGED, bare-metal-only): the verbose sibling of §7l's `font_ram_test`.  It runs the SAME `display_init()` → `display_load_fonts()` 8192-byte copy + byte-compare against `victor_font[]` (Test 6, 3429 non-zero bytes, 0 mismatches — the §7l coverage), but ALSO snapshots font RAM before/after the load (Tests 3/5, the "before" reading the MAME-reset state `00 00 00 00` since the bm_testhost preamble inits only bm_tty/bm_stdio, never display), reports the font geometry (Tests 1/2), and **dumps the glyph bit patterns** for space/A/'0' (Test 7).  **Its unique codegen over `font_ram_test`** is the glyph render loop — `for (bit = 9; bit >= 0; bit--) putchar((word & (1U << bit)) ? '#' : '.')` — a **VARIABLE left-shift by a loop counter** (the §4r variable-shift-count area) reading `uint16` glyph rows through a `volatile __far` pointer, exercised bug-loud against the deterministic native font shapes.  **ZERO code change this session** (not even build-glue): `font_test` resolves entirely through the SAME `display_init` → `bm_display_init` alias §7l added, so the only diffs are the battery entry `font_test:150:::` and the golden `minic/dos/tests/font_test.golden.txt` (95 lines, ends `PASS: All font tests completed successfully!`).  No timer values → golden toolchain-stable, bug-loud (broken load → `FAIL`/mismatch counts, corrupted glyph render → diffed `#`/`.` patterns).  **`font_layout_test` DECLINED** (the other printf-verbose font sibling): its only truly-unique codegen is the same `1<<bit` render loop (covered here) plus trivial constant integer arithmetic (`c+0x60`, `c*32`) exercised corpus-wide, and its ~230 output lines need a ~360-s budget — a poor trade for a standing gate.  SMALL (60,299 B code); bare-metal ONLY (no Victor font RAM on the DOS host); 95 lines at the §6f display-scroll rate need a 150-emulated-second budget (verified full output + hlt idle within budget).  FIRST-RUN PASS on MAME (verified `tools/test-newlibc.sh font_test` → [ok]).  Newlibc bare-metal support glue (MP does not link `bm_shim.c`) → no emit audit, no MP byte-compare; test-dos provably unaffected.  With font_test gated, the phase-3 bm_testhost-shaped upstream tests are EXHAUSTED except `interrupt_test` (SKIPPED, §6v) and `font_layout_test` (declined above).  **§7n (2026-06-14) STARTED Phase-6 milestone 6 — libstub retirement (the end-state, "newlibc replaces libstub as the real libc") — and proved the libstub-free architecture end-to-end for a DOS-hosted newlibc program; test-dos 320 → 321, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  `snprintf_test` now builds + runs DOS-hosted with ZERO libstub linked, byte-identical to its existing golden, exercising printf → _write → INT 21h through a runtime assembled entirely from newly-authored objects.  Background: the `--no-stdio` libstub builds only dropped libstub's *python stdio epilogue* (newlibc's printf replaced it) — the 2884-line `libstub.asm` body was still linked wholesale, supplying str/mem/ctype, the int86 DOS-syscall family, and the irreducible `_qbe_*` compiler-runtime helpers.  A new `--no-libstub` flag on `build-newlibc-test.sh` (small-model-only) splits those into three standalone objects linked **instead of** libstub: (1) **`minic/dos/qbe_rt.asm`** — the `_qbe_*` runtime (div32u/s, rem32u/s, huge_norm/add/sub/cmp, get_cs) + the `UDIVMOD32_BODY` macro, COPIED VERBATIM (near form) from `libstub.asm` (38-61 + 2229-2506); (2) **`minic/dos/dos_syscall.asm`** — int86/intdos/segread/int86x/intdosx, copied verbatim (self-contained: CS-relative SMC + function-local inline `dw` scratch); (3) **`minic/dos/newlibc/dos_libc.c`** — the minic-COMPILED libc fill (memcpy/memset/strlen/strcmp/strcpy/memcmp + the std-stream FILE objects `stdin/stdout/stderr`), the Phase-6 point: our compiler builds the libc newlibc itself lacks (phase-3 normally links newlib's libc.a here).  **STRATEGY = COPY, NEVER MUTATE:** all-new files; `libstub.asm`, `libstub_to_exe.py`, `crt0_exe.asm`, `omf_link.py`, and the entire existing `--no-stdio` path are UNTOUCHED, so MP/stevie/every existing gate provably cannot regress (gate green 321/321).  Accepted cost: `_qbe_*` + int86 logic now lives in TWO places (cross-reference comments in both new files).  Build-glue traps recorded: a pure-code asm TU must NOT declare `group DGROUP _DATA _BSS` (nasm errors — crt0 declares it for the link; code-only TU just `segment _TEXT class=CODE align=2 use16`); the std streams come from dos_libc.c since libstub no longer provides the sentinels (`FILE = { int _file; }`, printf_wrappers' stream_fd uses pointer identity + fd); a simple printf test reaches NO malloc (heap deferred — no `call _malloc`, no `call .*_far_` in the small `.omf.asm`).  Gate: new `test-dos.sh` entry "newlibc libstub-free (snprintf_test)" diffs the SAME golden (bug-loud: a missing runtime symbol fails the link, a wrong `_qbe_*` decimal conversion diffs the golden).  **§7o next (continue retirement):** (1) malloc/free + a BSS `char __heap[N]` via newlibc `_sbrk` (libgloss/syscalls.c:85-105), gated by a malloc-using test, MINDING the small-model DGROUP-64 KB invariant (omf_link.py:1290 — code is a separate `_TEXT`, but stack+statics+heap share one 64 KB DGROUP); (2) widen `--no-libstub` across the small NEWLIBC_TESTS (grow dos_libc.c per undefined-symbol errors), then to medium (qbe_rt/dos_syscall are near-form → need far-call ABI); (3) eventually retire `libstub_to_exe.py`'s python printf engine outright.  **§7o (2026-06-14) CONTINUED libstub retirement — widened `--no-libstub` across the ENTIRE small NEWLIBC_TESTS set AND added malloc/free + a real BSS heap; test-dos 321 → 334, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  Two increments: (1) **generality** — all 13 small NEWLIBC_TESTS (snprintf + six FAT/VFS + ramfs + stdio_route + bss + terminal_meta + fat_victor_label + block) now build and run libstub-free, each byte-identical to the SAME golden as its libstub build, proving §7n's 6-function `dos_libc.c` (memcpy/memset/strlen/strcmp/strcpy/memcmp) covers the WHOLE portable FAT/VFS/ramfs/stdio/block surface with ZERO new libc functions needed (a clean link wasn't enough — every one was DOSBox-verified output-equivalent); the §7n snprintf-only gate entry became a loop over `NEWLIBC_TESTS`.  (2) **malloc capability** — newlibc's portable subset has NO allocator of its own (phase-3 links newlib's libc.a for malloc/free), so `dos_libc.c` gained the canonical K&R free-list `malloc`/`free`, backed by newlibc's own `_sbrk` (`libgloss/syscalls.c`, already linked) carving from a real **BSS heap** in the all-new `minic/dos/heap.asm` — which exports the `__heap_start`/`__heap_end` bracket symbols (asm `___heap_start`/`___heap_end`) that `_sbrk` references, with `__heap_end`'s ADDRESS made exactly end-of-heap by controlled symbol placement (the one thing C-with-two-arrays can't guarantee, hence asm; HEAP_SIZE default 8 KB, well under the small-model 64 KB DGROUP).  **One conflict resolved:** `dos_shim.c` had carried §6b 2-byte placeholder `__heap_start`/`__heap_end` (link-satisfaction stubs, "documentedly NOT a usable heap") — now `#ifndef NO_LIBSTUB`-guarded so the libstub build keeps them but the `--no-libstub` build takes the real heap from heap.asm (build-newlibc-test.sh passes `-DNO_LIBSTUB` to every `--no-libstub` compile_unit; `--gc-sections` drops the heap entirely from any build that never reaches malloc, so the 12 non-malloc tests' data+bss is unchanged at 4322 B).  Gated by the all-new `malloc_probe` (`minic/dos/newlibc/malloc_probe.c`, a qbe-local probe — build-newlibc-test.sh's source resolver gained a `minic/dos/newlibc/` fallback after the newlibc-tree lookup): bug-loud over no-clobber block overlap, free-list reuse keeping live blocks intact, heap exhaustion (`malloc(60000)` on an 8 KB heap) returning NULL cleanly via `_sbrk`'s `__heap_end` bound, and a string round-trip — DOSBox-verified `noclobber ok / liveintact ok / exhaust ok / string victor 9000 / malloc_probe done`.  **STRATEGY unchanged (COPY/ADD, NEVER MUTATE):** heap.asm + malloc_probe.c are all-new; dos_libc.c only grew; dos_shim.c's change is a guarded omission; libstub.asm / libstub_to_exe.py / crt0_exe.asm / omf_link.py / the existing `--no-stdio` path are all UNTOUCHED, so MP/stevie/every existing gate provably can't regress (gate green 334/334; MP not rebuilt — links none of these files).  **§7p next:** widen `--no-libstub` to MEDIUM model (qbe_rt/dos_syscall are near-form — medium needs the far-call ABI: route them through a libstub_to_exe-style +2/retf rewrite OR author medium variants); then eventually retire `libstub_to_exe.py`'s python printf engine for non-newlibc programs.  **§7p (2026-06-14) DID the medium widening — the two MEDIUM FAT-write tests now run libstub-free; test-dos 334 → 336, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  The medium model far-calls the runtime helpers (`call far _qbe_div32u`/`_qbe_rem32u`/`_int86`), so the near-form `qbe_rt.asm`/`dos_syscall.asm` (plain `ret`, args at `[bp+4]`) needed the far-call ABI.  Chose the documented `libstub_to_exe.py` pattern in a small DEDICATED tool over a third hand-authored copy: the all-new `tools/near_to_far_rt.py` mechanically rewrites a near-form runtime asm TU to the far ABI — `ret`→`retf`, every positive `[bp+N]`→`[bp+N+2]` (the far return CS occupies an extra word; `[bp-N]`/`[bx+N]`/`[cs:...]` untouched — only `[bp+(\d+)]` matches, the `shift_bp_offset`/`^ret\b` logic copied from `libstub_to_exe.py`), and renames the near `_TEXT` to a UNIQUE far-code segment (`QBE_RT_TEXT`/`DOS_SYSCALL_TEXT`, matching `asm_to_omf.py`'s per-module `<BASE>_TEXT` naming so omf_link — which coalesces CODE by NAME — keeps each in its own paragraph/CS for the `call far` fixups; near `_TEXT` would coalesce into the single small-model frame).  The near files stay the single source of truth (raw for small, transformed for medium at build time).  `heap.asm` (BSS heap = near data in DGROUP for both models) and `dos_libc.c` (compiled `minic -m medium` → code far, data near automatically) needed NOTHING.  Build glue: `build-newlibc-test.sh`'s `--no-libstub` small-only guard → `small|medium` + the medium transform step.  Verified: `fat_write_test`/`fat_write_unit_test` (medium `--no-libstub`) link clean (0 libstub in map, runtime resolves from the far-code segments) and run byte-identical (49 B / 59 B) to their existing goldens; gated as `newlibc medium libstub-free (<t>)` (bug-loud: a wrong far-ABI rewrite corrupts the stack → diff, an unresolved symbol fails the link).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `near_to_far_rt.py` is all-new; all the asm/.c/python toolchain files are UNTOUCHED, so MP/stevie/every gate provably can't regress.  Trap: `_qbe_get_cs` (`mov ax, cs`) returns qbe_rt's OWN segment under a far call (wrong for far ISR-IVT entries, but matches the libstub medium behavior and no DOS-hosted medium consumer uses it).  **§7q (2026-06-14) EXTENDED `--no-libstub` to the ordinary `build-example.sh` path — a NORMAL (non-newlibc) minic program (its own `main()`, compiled in the build-example regime against `minic/include/` headers, the path stevie + plain examples take) now runs libstub-free; test-dos 336 → 340, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  §7n/§7o/§7p had retired libstub only for the newlibc TEST TREE (built by `build-newlibc-test.sh` against newlibc's own shiminc headers); §7q proves a normally-built program links the same way.  **Key finding (why it works):** `minic/include/stdio.h` declares `extern int printf()` (K&R, cdecl) — call-compatible with newlibc's `printf` (both cdecl variadic → `_printf`; under medium the cross-TU `call far _printf` matches as every medium newlibc test already proves).  So an example compiled in the build-example regime (TURBOC headers) calls `_printf`, resolved to newlibc's `printf_wrappers.c` definition (printf → `_write` → `vfs_write` → `dos_shim` INT 21h); the two compile regimes (example = TURBOC/`minic/include`; support TUs = `clang -E -D__ia16__`/shiminc+newlibc headers) meet ONLY at the linker.  **Implementation (`build-example.sh` `--no-libstub`, small|medium, additive/flag-guarded — the default libstub path is byte-unchanged):** (1) the example TU is compiled with `-Dmain=newlibc_test_main` (via a new `EXAMPLE_DEFS`) so `dos_shim.c`'s `main()` runs `vfs_init()` first (printf can't reach the console until the VFS device table is up); (2) a new `compile_newlibc_unit` helper (newlibc regime, mirrors `build-newlibc-test.sh`'s `compile_unit`) compiles the REUSED portable stdio stack (printf_wrappers, scanf_wrappers, syscalls, reent_stubs, dirent, unlink, vfs, fat, block, dos_shim, dos_libc — nothing new authored); (3) the runtime is `qbe_rt`/`dos_syscall` (assembled raw for small, rewritten to the far ABI by `near_to_far_rt.py` for medium, exactly as §7p) + `heap.asm`, NOT libstub; (4) `--gc-sections` strips the FAT/block code the printf-only program never reaches (12 of 16 modules dead-stripped).  **Gate:** NEW `minic/dos/examples/printf_nolibstub_probe.c` (a plain program — printf `%d`/`%u`/`%x`/`%c`/`%s`/`%ld` + malloc/free/strcpy round trip), gated FOUR ways in `test-dos.sh` — small + medium × {libstub, libstub-free} — all diffing ONE golden (`printf_nolibstub_probe.golden.txt`); the libstub build is the EQUIVALENCE ANCHOR (libstub's python printf), so a divergent `_qbe_*`/printf conversion reds only the libstub-free entry while an unresolved libc symbol fails its link.  All four FIRST-RUN PASS; FULL gate green **340/340**.  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `printf_nolibstub_probe.c` + its golden are all-new; `build-example.sh`'s change is an additive flag branch; `libstub.asm`/`libstub_to_exe.py`/`crt0_exe.asm`/`omf_link.py`/`near_to_far_rt.py`/the existing libstub path are UNTOUCHED, so MP/stevie/every existing gate provably can't regress (MP NOT rebuilt — links none of these files).  **Trap recorded (cost ~20 min): the stale-binary illusion** — `libstub.asm` provides `_printf` but NOT `_puts`; the probe first used `puts()`, so the libstub (anchor) build FAILED at link (undefined `_puts`) — but the prior libstub-free build's `.exe` was still in `OUT_DIR`, so `run-dos-exe.sh` ran the STALE binary → identical output → it LOOKED like the libstub build worked.  Lesson: `rm -f` the target `.exe` before a build you mean to verify, and check the build's exit code, not just the run output.  Fix: the probe uses printf only (no puts), so both runtimes link.  **§7r next:** the larger end-state remains — retire `libstub_to_exe.py`'s python printf engine for stevie ITSELF (a much bigger TU than a probe; likely needs `build-stevie.sh --no-libstub` + a review of stevie's TURBOC-isms against the newlibc stdio surface); far-DATA models (compact/large/huge) `--no-libstub` (far_stdlib-aware stdio + far-pointer libc fill) still await a consumer.  **§7r (2026-06-14) RETIRED libstub for STEVIE ITSELF — the full 24-TU editor builds AND runs libstub-free as a medium .EXE, behind a gateable `dos_libc.c` libc-surface expansion; test-dos 340 → 344, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  §7q proved the libstub-free `build-example.sh` path on a small printf+malloc probe; §7r is the headline end-state — a real 146 KB program with NO libstub — done GATE-FIRST per the user (stevie is interactively verified, not auto-gateable, so the reusable libc fill it needs lands behind a bug-loud probe + golden first).  `dos_libc.c` grew with the stevie libc surface beyond §7n/§7o's mem/str/malloc: real `strncmp`/`strchr`/`strrchr`/`strcat`/`strncpy`/`strcspn` + the ctype family (`isalpha`/`isdigit`/`isspace`/`islower`/`isupper`/`toupper`/`tolower`) + `atoi`/`getenv`/`system`/`signal`/`exit`/`chmod`/`mktemp`/`delay`/`sleep` + `getc`/`remove` — each matching **libstub's exact behavior** (the equivalence anchor): the real funcs for real, the libstub stubs (`atoi`→0/`getenv`→NULL/`system`→0/`signal`→NULL) stubbed identically, so libstub-free stevie stays behavior-identical to the verified libstub stevie.  **KEY TRAP: the .EXE libstub OVERRIDES the .COM stubs** — `libstub_to_exe.py` replaces libstub.asm's `.COM`-path `getc`(`mov ax,-1`)/`remove`(`mov ax,0`) stubs with REAL impls (buffered getc, real unlink), so the .EXE anchor expects working versions; matched by `getc`→`fgetc` / `remove`→`unlink` (NOT the `.COM` stub values), and `getc(stdin)` is therefore NOT probed (the real EXE getc blocks on console).  `rename` is NOT in `dos_libc.c` (newlibc's `rename.c` provides it; a copy duplicate-symbol-collides with the medium fat_write tests that link rename.c — the regression that briefly reded the gate; fixed by dropping it + adding rename.c to build-stevie's support set).  Gated by the all-new `minic/dos/examples/dos_libc_probe.c` (semantic/bucketed results so libstub & libstub-free agree by construction) FOUR ways in `test-dos.sh` (small + medium × {libstub anchor, libstub-free}, one golden), all first-run identical.  Stevie: `build-stevie.sh --no-libstub` mirrors `build-example.sh --no-libstub` (newlibc portable stdio + the dos_libc fill + `qbe_rt`/`dos_syscall`/`heap`, `--gc-sections` strips 20 unused FAT/block segments); `dos_shim.c`'s `main()` now forwards `argc`/`argv` (stevie's K&R `main(argc,argv)` switches on `argv[1][0]`, so garbage args would crash; the newlibc tests ignore the extra cdecl args, output-neutral); a `STEVIE_HEAP_SIZE` knob (32 KB default — data+bss 58,206 B + 4 KB stack < 64 KB DGROUP) sizes the BSS heap.  Stevie builds libstub-free (medium, code 123,648 B multi-CS, image 193,840 B) and its startup screen (`Empty Buffer` + vi `~` tildes, rendered through the newlibc VFS/console write path) is **BYTE-IDENTICAL** to the libstub baseline's; interactive editing/save verification on Victor/DOSBox is handed to the user (stevie reads keys via INT 21h AH=07h, not redirectable stdin).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE — `dos_libc_probe.c`+golden all-new, `dos_libc.c`/`dos_shim.c`/`build-stevie.sh` additive; libstub.asm/libstub_to_exe.py/crt0_exe.asm/omf_link.py/near_to_far_rt.py/qbe_rt.asm/dos_syscall.asm/heap.asm UNTOUCHED → MP/the libstub stevie/every gate provably can't regress).  Trap: `link.err` is append-not-truncate, so a stale `undefined symbols` block misleads — read the LAST block / check the build exit code.  **§7s next:** stevie interactive Victor verification (open/edit/`:w`/`:q`; `system`/`getenv` are no-op stubs so `:!`/`$COMSPEC` won't work, `rename` is now newlibc-real, the 32 KB heap caps file size); far-DATA `--no-libstub` (awaits a consumer); the ultimate end-state — make `--no-libstub` the default (retire `libstub_to_exe.py`'s python printf outright).  **§7s (2026-06-14) FIXED libstub-free file I/O — stevie now LOADS and SAVES real DOS files; test-dos 344 → 348, no compiler/qbe/emit/minic source touched.**  §7r left this broken (the smoke test only reached the input loop, never a real file): a normal program's `fopen`/`getc`/`fputs` route through newlibc stdio → `_read`/`_write` → `vfs_*`, and newlibc's `vfs.c` is the BARE-METAL VFS (FAT-on-block/SASI + ramfs) with no backend for a plain DOS path (`config.sys` → "New File" on load, "Can't open file for writing!" on save).  FIX: a new `minic/dos/newlibc/dos_vfs.c` implements the `vfs_*` surface (open/close/read/write/lseek/stat/fstat/isatty/unlink/rename + dir stubs) via DOS INT 21h (AH=3Dh/3Ch/3Fh/40h/3Eh/42h/41h/56h); the fd IS the DOS handle (0/1/2 are CON, so printf's `_write(1)`/getchar's `_read(0)` are unchanged; files get handles ≥5) — no fd table, no FAT/block/ramfs, exactly what libstub did directly but behind newlibc's stdio.  Linked INSTEAD of `vfs.c`+`fat.c`+`block.c` on the `build-example.sh`/`build-stevie.sh` `--no-libstub` path (the real-DOS-program path); `build-newlibc-test.sh` is UNCHANGED — its RAM/FAT tests still use the bare-metal `vfs.c` (they test the VFS/FAT machinery itself).  Dropping fat/block shrank stevie code 123,648 → 84,627 B.  Gated by the all-new `minic/dos/examples/dos_file_probe.c` (fopen"w"+fputs/fputc → fclose → fopen"r"+getc to EOF → remove → reopen), 4-way like dos_libc_probe (small + medium × {libstub anchor, libstub-free}, one golden, all first-run identical — both do real INT 21h file I/O).  Verified on stevie itself: `stevie TFILE.TXT` in DOSBox now renders the file + status `"TFILE.TXT" 3 lines, 34 characters` (was "New File").  STRATEGY unchanged (COPY/ADD, NEVER MUTATE — `dos_vfs.c`/`dos_file_probe.c`/golden all-new; the build scripts swapped support-set TUs on the `--no-libstub` branch only; newlibc's `vfs.c`/`fat.c`/`block.c`/`syscalls.c` + libstub UNTOUCHED → the newlibc FAT/VFS gate + MP + the libstub stevie can't regress).  Traps: `rename.c` calls `vfs_rename` so dos_vfs provides it (AH=56h needs ES:DI → `segread`+`int86x`); `off_t` is 16-bit so `vfs_lseek` returns the low word only (fine — stevie is sequential).  **A second bug kept SAVE broken (fixed same session):** `:x`/`:w` on an existing file gave `"(null)"` + unchanged file (load + `:w NEWFILE` worked).  `struct stat` differs in size across the two regimes meeting at `stat()` — stevie's minic/include (~8 B) vs newlibc shiminc (~30 B) — and `vfs_stat` zeroing `sizeof(*st)` overflowed `writeit`'s ~8 B stack `statbuf`, clobbering `fname` to NULL (only when `stat` succeeds = an existing file; a new file's `stat` fails early, so `:w NEWFILE` worked; libstub's `_stat` is a `-1` no-write stub, so the libstub build worked).  Fix: `vfs_stat`/`vfs_fstat` write NOTHING, return status only (stevie uses `statbuf` only for a no-op `chmod`; the cross-regime struct has no shared ABI).  Gated via `dos_file_probe`'s `stat_keeps_fname()` canary (bug-loud — the overflow crash-loops `main`).  Verified: stevie save now writes the edited file correctly.  LESSON: passing a struct by pointer across the minic/include vs shiminc regimes is an ABI landmine.  **§7t next:** deeper stevie verify (edit→`:w`→reload, multi-file, tags); far-DATA `--no-libstub` (far-pointer dos_vfs + stdio); make `--no-libstub` the default.  No QBE backend bug currently open.  **§7t (2026-06-15) WIDENED `--no-libstub` to the far-DATA models compact + large — `printf_nolibstub_probe`, `dos_libc_probe`, AND `dos_file_probe` all run libstub-free (byte-identical to their existing goldens) under compact and large; test-dos 348 → 360, no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare).**  §7q/§7p had the libstub-free path on small (near runtime) + medium (far code, near data via `near_to_far_rt.py`).  §7t covers the far-DATA tier.  **Key discovery — in THIS backend compact/large/huge are ALL far code + far data** (minic `NEAR_CODE()` excludes MCompact, `minic.y:46`; the compact probe emits `call far`/`retf`), so the far-data path is uniform, NOT the textbook compact=near-code split.  Two mismatches vs medium, both fixed COPY/ADD (all-new files, no compiler touch): (1) **far-pointer syscall ABI** — `int86`/`intdos`/`segread` mangle to `_far_int86`/… (minic `far_stdlib[]`) and must read their `union REGS`/`struct SREGS` through the caller's FAR segment (ES:BX), so the new **`minic/dos/dos_syscall_far_data.asm`** ports libstub_to_exe.py's `FAR_DOSIO_EXE` `_far_int86`/`_far_intdos`/`_far_segread` VERBATIM + a far-pointer **`_int86x`** (int86x is NOT in `far_stdlib[]` → its call site is the unmangled `_int86x` with far-ptr args; `dos_vfs.c`'s `vfs_rename` needs it, and `dos_vfs` is ONE CODE segment so `vfs_write`/printf keeps `vfs_rename`+its `_int86x` live through `--gc-sections`); linked INSTEAD of the near-data `dos_syscall.asm` under far data.  (2) **far_stdlib name bridges** — printf/str*/mem* call sites mangle to `_far_X`, but newlibc/dos_libc DEFINE the plain `_X` (a C definition emits its real name; only call sites mangle) and, compiled `-m compact`, those plain defs ALREADY have the correct far-pointer ABI, so the only gap is the NAME.  The new **`minic/dos/far_stdlib_bridge.asm`** bridges each with a far tail-call `_far_X: jmp far _X` (a `jmp`, NOT a `call far` thunk — preserves the variadic printf arg frame; verified `jmp far _sym` resolves through omf_link's location-3 far fixup exactly like `call far`), each thunk in its own CODE segment so `--gc-sections` drops the unreferenced ones.  `qbe_rt` is still the `near_to_far_rt.py` far-code rewrite (same as medium); `heap.asm` (near-data BSS heap) unchanged.  **huge is NOT yet supported libstub-free**: printf + the far bridges + far int86 all work under huge, but `malloc` returns NULL — newlibc's `_sbrk` brackets the heap with `next_heap > __heap_end`, a huge-model pointer compare against the UNNORMALIZED `__heap_end` symbol address that the huge-pointer path mis-evaluates (the §7g/§4s huge-normalization family; malloc_probe only ever ran small-model so this heap path was never huge-exercised).  The huge *libstub* build is unaffected (libstub's own malloc), so it's a real but bounded compiler/runtime investigation, deferred; `build-example.sh` rejects `--no-libstub --model=huge` with a precise message.  Gate: the three §7q/§7r/§7s probes' model loops in `test-dos.sh` grew `small medium` → `small medium compact large` (each adds compact+large × {libstub anchor, libstub-free}, 12 new entries, all FIRST-RUN identical to the existing goldens — the libstub build is the equivalence anchor, a wrong far-pointer ABI corrupts the syscall/printf and diffs the golden, a missing bridge fails the link).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `dos_syscall_far_data.asm`/`far_stdlib_bridge.asm` are all-new; `build-example.sh`/`test-dos.sh` changes are additive (far-data runtime branch + wider model loops + guard); `libstub.asm`/`libstub_to_exe.py`/`near_to_far_rt.py`/`dos_syscall.asm`/`qbe_rt.asm`/`heap.asm`/`dos_vfs.c`/newlibc are UNTOUCHED, so MP/stevie/every existing gate provably can't regress (MP NOT rebuilt — links none of these).  **§7u next:** (1) huge libstub-free — fix the `_sbrk` huge-pointer heap-compare (`__heap_end` unnormalized-symbol-address compare; needs an emit-audit + MP byte-compare since it's likely a compiler/runtime fix); (2) `build-stevie.sh --no-libstub --model=compact/large` (stevie far-data — the file path is now proven by `dos_file_probe` compact/large, incl. int86x rename + vfs_stat); (3) the end-state — make `--no-libstub` the default (retire `libstub_to_exe.py`'s python printf), which needs huge done + broad re-verification.  Carried compiler tracks (await a consumer): the aoa sub-gaps.  Bare-metal phase-3 bm_testhost tests EXHAUSTED.  **§7u (2026-06-15) COMPLETED huge libstub-free — fixed the `_sbrk` MHuge pointer-relational heap-compare so malloc + the whole heap chain works under huge with NO libstub; the three build-example probes (`printf_nolibstub_probe`/`dos_libc_probe`/`dos_file_probe`) now run libstub-free byte-identical to their goldens under huge too; test-dos 360 → 366; the fix is a frontend MHuge-gated `minic.y` change → no emit audit; MP compact body 731,088 byte-identical → no Victor run.**  §7t deferred huge: newlibc's `_sbrk` (libgloss/syscalls.c) brackets the heap with `next_heap < __heap_start || next_heap > __heap_end`, and under huge those relational compares were emitted as a FLAT 32-bit unsigned `cultl` of the seg:off words — correct ONLY when BOTH operands are NORMALISED huge pointers (the seg:off word is then monotonic in linear address).  But `__heap_start`/`__heap_end` are bare symbol addresses (`$sym`, raw `DGROUP:offset`, offset can exceed 0xF = UNNORMALISED) while `next_heap` went through `_qbe_huge_add` (normalised), so `cultl normalised, unnormalised` mis-ordered them → `malloc` returned NULL (the §7g/§4s huge-normalization family; `malloc_probe` had only ever run small, so this heap path was never huge-exercised).  **FIX (minic.y, the `Binop` default arm, ~33 lines):** under `memmodel == MHuge` a pointer RELATIONAL compare (`p < q`, `p <= q`; the parser already lowers `p > q`/`p >= q` to swapped-operand `<`/`<=`) with BOTH operands pointers (non-function) now routes through `_qbe_huge_cmp(p, q)` — the SAME helper the MHuge ptr−ptr-subtraction block already uses, which recomputes `seg*16+off` from the raw words → `linear(p)−linear(q)`, NORMALISATION-INVARIANT — and tests its sign (`p<q ⟺ csltl cmp,0`, `p<=q ⟺ cslel cmp,0`).  MHuge-gated, so compact/large/near keep the flat `cultl` (verified compact still emits `cultl`, no `huge_cmp`) → the MP compact corpus is byte-identical and the §7t compact/large probes untouched; conflicts UNCHANGED at 115 (action-only).  Equality (`==`/`!=`) of two non-null huge pointers from different normalisations carries the SAME latent flat-compare gap, but `_sbrk` only does `== NULL` (0:0, linear 0 — fine), so it stays unfixed (no consumer; relational was the bug).  **Gate:** `build-example.sh` dropped its `--no-libstub --model=huge` rejection (the §7t far-DATA runtime branch already handled huge identically to compact/large — only the guard blocked it); the three §7t probe loops in `test-dos.sh` grew `small medium compact large` → `+ huge` (6 new entries: 3 probes × {libstub anchor, libstub-free}).  BUG-LOUD verified: reverting the minic fix makes the unfixed compiler emit `cultl` and `printf_nolibstub_probe` (it does a malloc/strcpy round trip) print `FAIL: malloc returned NULL` under huge.  All 366 green; every existing huge probe (hugeprobe, gc_bigheap_probe, huge_norm_probe, the caddr family, …) still passes → no huge codegen regression.  STRATEGY: the COPY/ADD-NEVER-MUTATE toolchain (libstub.asm, libstub_to_exe.py, near_to_far_rt.py, qbe_rt.asm, dos_syscall*.asm, far_stdlib_bridge.asm, heap.asm, dos_vfs.c, newlibc) is UNTOUCHED; the only source change is the MHuge-gated minic.y arm + two additive build/gate edits, so MP/stevie/every existing gate provably can't regress.  **§7v (2026-06-15) WIDENED `build-stevie.sh --no-libstub` to the far-DATA models compact + large + huge — the full 24-TU stevie editor now builds libstub-free as a compact/large/huge .EXE, and its startup screen is BYTE-IDENTICAL to the libstub anchor at each model AND to the §7r medium baseline; no compiler/qbe/emit/minic source touched (→ no emit audit, no MP byte-compare); test-dos UNCHANGED at 366/366.**  §7r had stevie libstub-free at medium; §7t/§7u had already proven the far-DATA libstub-free RUNTIME (qbe_rt far rewrite + `dos_syscall_far_data.asm` ES:BX far-pointer int86 family + `far_stdlib_bridge.asm` `_far_X: jmp far _X` name bridges + the §7u MHuge `_sbrk` heap-compare fix) byte-identical to goldens under compact/large/huge via the three build-example probes, and the stevie far-data FILE path (fopen/fputs/getc/remove/`stat` incl. the §7s `int86x` rename + `vfs_stat` no-write) is exactly what `dos_file_probe` exercises — so §7v is the build-glue pointing stevie at that already-proven runtime.  **The change (`tools/build-stevie.sh` ONLY, +33/-11):** (1) the `--no-libstub` guard relaxed from `small|medium`-only to reject ONLY `tiny` (the .COM-only model); (2) the runtime block, previously two-way (`small` raw / `else`=medium near_to_far), became the SAME three-way as `build-example.sh` — `small` (raw near qbe_rt/dos_syscall), `medium` (qbe_rt + dos_syscall both `near_to_far_rt.py`, near data), and `compact|large|huge` (qbe_rt far rewrite + `dos_syscall_far_data.asm` INSTEAD of the near dos_syscall + `far_stdlib_bridge.asm`, all far data); `crt0_exe` already branched FAR_DATA for compact/large/huge, the newlibc support TUs already compile `-m $MODEL`, and `heap.asm` + `--gc-sections` were unchanged.  **Verification (no new gate entry — the §7r pattern, since stevie is interactively verified not auto-gateable):** all three far-data models link clean (0 libstub symbols in the map, 37 far-bridge/far-syscall symbols present; compact/large code 207,821 B + data+bss 56,996 B + 4 KB stack < 64 KB DGROUP, comfortable now that §7s dropped fat/block; huge code 224,714 B), and — because stevie renders its screen through the newlibc console write path (`write(1)` → dos_shim → INT 21h AH=40h, captured by `run-dos-exe.sh`'s `> OUT.TXT`) — running stevie under DOSBox (it renders the full vi startup then blocks on a keypress; the idle block trips run-dos-exe's timeout, which dumps the COMPLETE rendered screen) shows the `Empty Buffer` status + `~` tildes + Victor `^[Y` cursor escapes BYTE-IDENTICAL (153 B) across {compact,large,huge} × {libstub anchor, libstub-free} and equal to the compact baseline.  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): only `build-stevie.sh`'s `--no-libstub` branch changed (default libstub path byte-unchanged — `make check` green, full test-dos 366/366 incl. the default-medium stevie size gate); all the far-data runtime asm/python/C (qbe_rt/dos_syscall_far_data/far_stdlib_bridge/heap/dos_vfs/dos_libc/near_to_far_rt + libstub.asm/libstub_to_exe.py) is UNTOUCHED, so MP/the libstub stevie/every gate provably can't regress.  Interactive edit/save on the far-data libstub-free stevie is keyboard-bound (INT 21h AH=07h, not redirectable) → handed to the user, exactly as §7r/§7s did for medium.  **§7w next:** the ultimate end-state — make `--no-libstub` the DEFAULT (retire `libstub_to_exe.py`'s python printf engine outright), now that every model (small/medium/compact/large/huge) is proven libstub-free for BOTH the build-example probes AND stevie: flip the default in `build-example.sh`/`build-stevie.sh`, keep the libstub anchor reachable as the golden cross-check during transition, re-verify every model/program, and confirm MP (links none of this — stays libstub) + stevie still byte-match.  Carried compiler tracks (await a consumer): the aoa sub-gaps; huge pointer EQUALITY flat-compare (the §7u relational fix's latent sibling — `==`/`!=` of two differently-normalised huge pointers; no consumer, `_sbrk` only does `== NULL`).  **§7w (2026-06-15) MADE `--no-libstub` THE DEFAULT — the end-state of the Phase-6 libstub-retirement campaign: `build-example.sh` and `build-stevie.sh` now build libstub-free by DEFAULT, with `--libstub` the opt-out equivalence anchor; the whole 287-probe codegen gate, the §7q/§7r/§7s program probes, AND stevie all run libstub-free; test-dos GREEN 366/366, no compiler/qbe/emit/minic source touched (→ no emit audit; MP provably unaffected — it links NONE of the changed files and builds via `libstub_to_exe.py` directly, so NO MP byte-compare).**  `NO_LIBSTUB` now defaults to 1 in both scripts; `--libstub` sets it 0 (the anchor, kept reachable as the golden cross-check), `--no-libstub` still accepted (re-asserts the default).  Because libstub-free is .EXE-only, needs the newlibc tree, and cannot build tiny, both scripts auto-fall-back to libstub when the model is tiny / on the stevie .COM path / the newlibc tree is absent — but ONLY when libstub-free was the implicit default; an EXPLICIT `--no-libstub` keeps the strict behavior (error on tiny, `exit 77` skip when the tree is absent).  Two FILL increments, COPY/ADD only: `dos_libc.c` grew `strstr`/`memmove`/`fflush`/`ftell`/`bdos` (the ONLY libc gaps across a build-only sweep of every probe×model combo; `ftell`/`fflush` return 0 matching libstub's stubs, `bdos` is C over `int86` so one def fits all models), `far_stdlib_bridge.asm` grew the `strstr`/`memmove`/`fflush` far bridges (the rest are not in minic's `far_stdlib[]`).  Two PIN sets stay on `--libstub` (`build_runtime_probe` case): (1) printf divergence — newlibc's `tiny_vformat` formats `%p` as `0x`-prefixed + 16-bit-truncated and does NOT implement `%o` (echoes the literal), so `cstrprobe`/`compactprobe_extra`/`huge_norm_probe`/`mediumprobe` would diverge from their libstub-captured goldens; (2) `setjmp`/`longjmp` have NO libstub-free impl yet (they need a far `jmp_buf` + register save/restore in asm — deferred, see `far_stdlib_bridge.asm`), so `setjmp_probe`/`setjmp_clobber_probe`/`arr_jmpbuf_probe`/`aoa_extended_probe`/`split_stack_probe` pin too.  (Static format-specifier grep over-predicts — `phase_bprime_probe`/`farlocal_probe` use `%p`/`%o` but their values match, so the pin set is the EMPIRICAL build+diff result, not the grep.)  The gate's §7q/§7r/§7s probe pairs were rewired (anchor → `--libstub`, default → libstub-free, both diffing the same golden), and `run_stevie_size` pinned `--libstub` (it is a LIBSTUB size-regression detector and its budget is libstub-calibrated; the libstub-free stevie is verified separately).  VERIFIED: full gate 366/366; stevie startup screen BYTE-IDENTICAL (152 B) libstub-free-default vs `--libstub` anchor (the §7r/§7v rendered-screen byte-compare); `make check` green; all fallback paths correct (tiny→libstub .EXE, no-arg stevie→.COM libstub, newlibc-absent default→libstub, explicit `--no-libstub`+absent→exit 77).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `dos_libc.c`/`far_stdlib_bridge.asm` additive, the build scripts an additive flag + fallback branch (default libstub path byte-unchanged), `libstub.asm`/`libstub_to_exe.py`/`near_to_far_rt.py`/`qbe_rt.asm`/`dos_syscall*.asm`/`heap.asm`/`dos_vfs.c`/newlibc all UNTOUCHED.  COST NOTE: every codegen probe now compiles the full newlibc stack libstub-free, so the gate BUILD phase is markedly slower (a shared precompiled newlibc-support archive would recover it — a future optimization, not done here).  **§7x (2026-06-15) GAVE THE LIBSTUB-FREE RUNTIME ITS OWN `setjmp`/`longjmp` — the all-new `minic/dos/setjmp_rt.asm` supplies the setjmp surface the `--no-libstub` runtime lacked, so FOUR of the five §7w-pinned setjmp probes are UNPINNED and run libstub-free byte-identical to their goldens; test-dos GREEN 366/366, no compiler/qbe/emit/minic source touched (→ no emit audit; MP unaffected — links none of the changed files → no MP byte-compare).**  The new TU holds three `%ifdef`-selected ABI forms, each COPIED VERBATIM from `libstub_to_exe.py` — near (`NEAR_SETJMP_EXE`, small), `-dSJ_FAR_CODE` (`SETJMP_EXE`, medium: far code / near data, env near-ptr via DS:BX, `retf`), `-dSJ_FAR_DATA` (`FAR_SETJMP_EXE`, compact/large/huge: minic mangles → `_far_setjmp`/`_far_longjmp`, env 4-byte far ptr via ES:BX) — the two far forms in a unique `SETJMP_RT_TEXT` far-code segment so `call far` resolves (mirrors `near_to_far_rt.py`'s `QBE_RT_TEXT`).  Deliberately NOT routed through `near_to_far_rt.py` (its `[bp+N]+2` shift would corrupt the jmp_buf internal `[bx+10]`/`[bx+12]` ret-IP/CS offsets — the reason `libstub_to_exe.py` hand-writes the near form).  `build-example.sh` + `build-stevie.sh` link `setjmp_rt.obj` in all three runtime branches (additive; `--gc-sections` drops it when unused — stevie uses no setjmp, so its startup screen is still BYTE-IDENTICAL 152 B vs the `--libstub` anchor).  Gate: the four passing probes (`setjmp_probe`, `setjmp_clobber_probe`, `arr_jmpbuf_probe`, `aoa_extended_probe`) were UNPINNED in `build_runtime_probe`.  **`split_stack_probe` STAYS pinned for an UNRELATED reason** (re-documented): its `ok7` asserts `heapseg == dgroupseg`, which holds for libstub's single-base DGROUP heap but not the libstub-free BSS heap — `heap.asm`'s `_BSS` gets its own DGROUP paragraph (0x070D) distinct from `_DATA` globals (0x06F2) under far-data + `--split-stack`, a LAYOUT difference not a bug (its setjmp step `ok6` passes libstub-free).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `setjmp_rt.asm` all-new; `libstub_to_exe.py`/`libstub.asm`/`near_to_far_rt.py`/`qbe_rt.asm`/`dos_syscall*.asm`/`far_stdlib_bridge.asm`/`heap.asm`/`dos_vfs.c`/newlibc UNTOUCHED.  Gate-architecture note recorded: test-dos builds+stages ALL ~346 runtime probes first (no DOSBox), then runs the whole set in ONE DOSBox batch boot at the end (`flush_runtime_batch`) — only that run phase counts; a long DOSBox-less build stretch is NORMAL (and the §7w libstub-free default makes it markedly longer).  **§7y next:** a `%p`/`%o` shim for newlibc's `tiny_vformat` to unpin the 4 printf probes (`cstrprobe`/`compactprobe_extra`/`huge_norm_probe`/`mediumprobe`); OR the gate build-speed fix (precompile the newlibc support set into an archive — the real §7w cost); OR delete `libstub_to_exe.py`'s python printf for non-anchor use; OR make `split_stack_probe` pass libstub-free (give `heap.asm`'s `_BSS` the same DGROUP paragraph base as `_DATA` under far-data — an omf_link DGROUP-normalisation track).  Carried compiler tracks unchanged (await a consumer): the aoa sub-gaps; huge pointer EQUALITY flat-compare (the §7u relational fix's latent sibling).  **§7y (2026-06-15) PAID DOWN THE §7w GATE-BUILD-TIME REGRESSION — `tools/build-example.sh` now caches the ten libstub-free newlibc support TUs per memory model under `build/nl-cache/<model>` and reuses them across every probe in a run, cutting the full `tools/test-dos.sh` wall from 18m24 (the `NL_CACHE=0` baseline = the pre-§7y per-build recompile) to 8m06 — both GREEN 366/366, no compiler/qbe/emit/minic source touched (→ no emit audit; MP links none of the changed files → no byte-compare).**  §7w made libstub-free the default, so every codegen probe rebuilt the full newlibc portable-stdio stack (~2 s) even though those objs are byte-identical across all probes of a model (the support set depends only on (model, sources+headers, toolchain), never on the example).  The fix (build-example.sh ONLY, additive): the support TUs (`printf_wrappers`/`scanf_wrappers`/`syscalls`/`reent_stubs`/`dirent`/`unlink`/`rename` + `dos_vfs`/`dos_shim`/`dos_libc`) compile into a per-model cache keyed by a `shasum` stamp over the size+mtime of every dependency (the ten sources, `minic/dos/*.asm`, all headers under shiminc/`$NL`/minic-include, the minic+qbe binaries, asm_to_omf.py, near_to_far_rt.py, and build-example.sh itself); editing ANY of them invalidates the cache (no stale-obj trap, cf. §7q).  The stamp is written LAST (so `set -e` leaves no valid stamp on a failed compile) and a hit re-verifies every obj exists; `NL_CACHE=0` forces the pre-§7y per-`OUT_DIR` recompile (how the baseline was measured), `NL_CACHE_DIR` overrides the root.  VERIFIED: cache hit 0.51 s vs 2.68 s miss (≈5×), resulting `.exe` BYTE-IDENTICAL to a `NL_CACHE=0` build; `touch dos_libc.c` → MISS then HIT; per-model isolation; cold-cache gate 366/366 (8m06) and `NL_CACHE=0` baseline 366/366 (18m24); `make check` green.  Scope is just the build-example.sh codegen-probe path (the §7w regression); `build-newlibc-test.sh`'s own support compile predates §7w and is left for §7z.  STRATEGY (ADD, NEVER MUTATE): the `--libstub` anchor path, the tiny/.COM/absent-tree fallbacks, and every toolchain file (`libstub*.py`/`*.asm`/`dos_*.c`/newlibc) are UNTOUCHED; the cache dir lives under untracked `build/`.  **§7z next:** the `%p`/`%o` printf track (unpin the 4 printf probes — but it must edit newlibc's SHARED `printf_wrappers.c`, diverging from the upstream corpus; messiest); extend the per-model cache to `build-newlibc-test.sh`; delete `libstub_to_exe.py`'s python printf for non-anchor use; or make `split_stack_probe` pass libstub-free (the `heap.asm` `_BSS` DGROUP-base track).  **§7z (2026-06-15) EXTENDED THE §7y PER-MODEL SUPPORT-OBJ CACHE TO `tools/build-newlibc-test.sh` — the other support-recompile hot spot the §7w libstub-free default created; the full `tools/test-dos.sh` wall dropped from the §7y 8m06 to 6m33, both GREEN 366/366, no compiler/qbe/emit/minic source touched (→ no emit audit; MP links none of the changed files → no byte-compare).**  §7y cached the libstub-free support set for `build-example.sh` (the codegen-probe path) but explicitly left `build-newlibc-test.sh`'s own (heavier, test-varying) support compile — which recompiled its WHOLE portable-subset support set (printf/scanf wrappers, syscalls, reent_stubs, dirent, unlink, vfs, fat, block, dos_shim [+ fat_write/mkdir/rmdir/rename for write tests, + dos_libc under `--no-libstub`]) on every one of the ~30 newlibc-test builds the gate runs (13 small `NEWLIBC_TESTS` × {libstub, `--no-libstub`} + malloc_probe + the medium `fat_write` pair both ways), though those objs are byte-identical across every test of a given (model, libstub) pair.  **THE FIX (`build-newlibc-test.sh` ONLY, additive):** `compile_unit` gained an explicit `<out-dir>` 3rd arg (was hardcoded `$OUT_DIR`) so it writes either the per-test `OUT_DIR` (the test TU — the one TU that differs per build — and the `NL_CACHE=0` path) or a shared per-(model, libstub-mode) cache under `build/nl-test-cache/<model>[-nolibstub]`.  **Unlike §7y's FIXED 10-TU set, the support SUBSET varies per test** (fat_write tests add four TUs; `--no-libstub` adds dos_libc), so the cache is **per-TU with GAP-FILL**: a matching stamp means every obj already in the cache was built from the current inputs, and the loop then compiles only the TUs THIS test needs that aren't present yet (a fat_write test gap-fills its 4 extra into a cache a plain test seeded).  The stamp hashes a **FIXED SUPERSET** of every possible support source (test-independent — base 10 + fat_write group + dos_libc) plus the runtime asm, all relevant headers, `minic`+`qbe`, `asm_to_omf.py`, `near_to_far_rt.py`, and the script — so a fat_write test and a plain test compute the SAME stamp and SHARE one cache (a per-test `SUPPORT_TUS` hash would make them perpetually invalidate each other).  **Separate cache dirs per libstub mode** because `NL_DEFS` (`-DNO_LIBSTUB` vs empty) changes the objs (`dos_shim.c` is `#ifndef NO_LIBSTUB`-guarded).  On a stamp mismatch the whole key dir is `rm -rf`'d + rebuilt; the stamp is written LAST so `set -e` leaves no valid stamp on a failed mid-rebuild.  `NL_CACHE=0` forces the pre-§7z per-`OUT_DIR` recompile; `NL_TEST_CACHE_DIR` overrides the root (distinct default from §7y's `build/nl-cache` so the differently-compiled `dos_shim.obj` can't collide).  **VERIFIED:** cache hit 0.72 s vs cold 3.6 s (~5×); resulting `.exe` BYTE-IDENTICAL to an `NL_CACHE=0` build for both small libstub `snprintf_test` AND medium `fat_write_test`; `touch dos_shim.c` → MISS then HIT; four cache keys built independently (small 10 / small-nolibstub 11 / medium 14 / medium-nolibstub 15 objs), `dos_libc.obj` only in the `-nolibstub` keys, fat_write group gap-filled into `medium` without rebuilding the base 10; full gate 366/366 from a COLD cache (6m33), `make check` green.  STRATEGY unchanged (ADD, NEVER MUTATE): the libstub `--no-stdio` path, the per-test TU compile, and every toolchain file (`libstub*.py`/`*.asm`/`dos_*.c`/newlibc) are UNTOUCHED; the cache lives under untracked `build/`.  Both support-recompile hot spots (§7y build-example, §7z build-newlibc-test) are now cached — the §7w gate-build-time regression is fully paid down.  **§8a (2026-06-15) UNPINNED `split_stack_probe` FROM `--libstub` — the LAST setjmp-family pin (the §7w/§7x carry) is closed; the probe now runs libstub-free byte-identical to its golden under compact AND large, by teaching `tools/omf_link.py` to resolve EVERY far reference to a DGROUP member against the canonical DGROUP group frame; test-dos GREEN 366/366 (count unchanged — an unpin, not a new probe), no compiler/qbe/emit/minic source touched (→ no emit audit), MP compact BYTE-IDENTICAL (no Victor run), stevie startup screen byte-identical (152 B).**  `split_stack_probe`'s `ok7` asserts `heapseg == dgroupseg` (malloc memory in the SAME far segment as a global).  Under a far-data model + `--split-stack`, omf_link laid the libstub-free BSS heap (`heap.asm`'s `_BSS`) ABOVE `_DATA` inside DGROUP (`_DATA` para `0x06F2`, `_BSS` para `0x070D`) and resolved a far reference's SELECTOR to a DGROUP member against that member's OWN paragraph base — so `seg ___heap_start` (in `_BSS`) yielded `0x070D` while a global's `seg _g` (in `_DATA`, the LOWEST member = the group base) yielded `0x06F2` (the OFFSET was ALREADY group-framed, so the heap pointer was a self-consistent-but-non-canonical `0x070D:0x298`, valid 432 B into the heap region — usable, just not canonical, which is why libstub's single-base DGROUP heap never exposed it).  **FIX (`omf_link.py` `_frame_para`, ~3 edits, TOOLCHAIN not emit.c):** a far reference to a DGROUP member now resolves its frame to the canonical GROUP base (min member paragraph) for EVERY frame method (0 segment, 2 external, 5 target-frame) and EVERY location — method 5 previously group-framed only the 16-bit OFFSET (loc 1/5), not the SELECTOR (loc 2) or the 32-bit far ptr (loc 3); the loc-3 far-ptr OFFSET was generalized to be frame-relative (`tgt_abs_byte − frame_byte`, identical for the common frame==target-seg case).  NO-OP for the lowest member (`_DATA`, already the group base) and for ungrouped CODE / FAR_* segments → only non-lowest members like `_BSS` are normalized; offset and selector stay consistent because both derive from the one `_frame_para` result.  The heap reference is nasm target-frame (method 5), confirmed via a throwaway `OMF_DEBUG_HEAP` print; the method 0/2 edits are kept for rule-completeness though no current consumer exercises them, proven harmless by MP byte-identity.  Verified: MP compact built once then re-linked the SAME objs with OLD (git-stashed) vs NEW omf_link → `cmp` BYTE-IDENTICAL (image 751,664, body 731,088) ⇒ MP has zero far refs to a non-lowest DGROUP member, no Victor run; stevie rendered startup screen byte-identical 152 B (§7r/§7v pattern); the `--libstub` anchor still matches its golden; full gate 366/366.  Gate change: removed `split_stack_probe` from `build_runtime_probe`'s `libstubflag` pin (golden UNTOUCHED — it matched both engines already).  **§8b next:** the `%p`/`%o` printf track (unpin the 4 printf probes `cstrprobe`/`compactprobe_extra`/`huge_norm_probe`/`mediumprobe` — but it edits newlibc's SHARED `printf_wrappers.c`, diverging from the upstream corpus; messiest); or delete `libstub_to_exe.py`'s python printf for non-anchor use (the asm-runtime gaps — setjmp §7x, DGROUP heap §8a — are now all closed; the four printf-format pins are the only remaining libstub dependency).  **§8b (2026-06-16) CLOSED the `%p`/`%o` printf track — the LAST four `--libstub` pins (`cstrprobe`/`compactprobe_extra`/`huge_norm_probe`/`mediumprobe`) now run libstub-free byte-identical to their goldens, so NO probe pins `--libstub` anymore and the Phase-6 libstub-retirement campaign is COMPLETE; test-dos GREEN 366/366 (count unchanged — an unpin, not a new probe), no compiler/qbe/emit/minic source touched (→ no emit audit; MP links NONE of the changed files and builds via `libstub_to_exe.py` directly → no MP byte-compare).**  The four probes gate the §4i/§4s/§7g far/huge normalisation arithmetic against libstub-captured goldens via `%p`/`%o`, but newlibc's `tiny_vformat` (`printf_wrappers.c`) formats `%p` as a `0x`-prefixed width-4 hex and does NOT implement `%o` (it echoes the literal `"%o"`), so they pinned the libstub PYTHON-printf anchor.  **FIX (COPY/ADD, NEVER MUTATE — the dos_vfs.c pattern):** a new **`minic/dos/newlibc/dos_printf.c`** is a VERBATIM fork of newlibc's `printf_wrappers.c` with exactly two deltas — (1) **`%p`** matches libstub: raw pointer value, lowercase hex, NO `0x` prefix, zero-padded to the full pointer width (8 hex far / 4 hex near); since minic's `(uintptr_t)(void *)` cast DROPS the segment under a far-data model (yields only the offset), the far branch reads the 4-byte far-pointer arg straight as `va_arg(ap, unsigned long)` to recover the raw `(seg<<16)|off` image (so `1734:0007` → `17340007`, `(char*)0x12345678L` → `12345678`), while the near branch (`sizeof(void*)==2`) keeps the offset cast (→ `5678`); (2) **`%o`** is implemented (base-8, no prefix → `0777` prints `777`).  It is linked INSTEAD of `printf_wrappers.c` ONLY on the `build-example.sh` / `build-stevie.sh` `--no-libstub` path (the `NL_SUPPORT` swap), so newlibc stays pristine and the bare-metal (`bm_stdio`) + `build-newlibc-test.sh` paths keep newlibc's own `printf_wrappers.c` — their goldens (and the newlibc regression corpus, which uses no `%p`/`%o`) are byte-untouched.  Gate: removed the `cstrprobe|compactprobe_extra|huge_norm_probe|mediumprobe` `libstubflag="--libstub"` case in `test-dos.sh` (goldens UNTOUCHED — they were libstub-captured and the libstub-free path now reproduces them).  VERIFIED: all four probes byte-identical to their goldens libstub-free across every gated model (small near-`%p`=`5678`, compact/large/huge far-`%p`=8-hex-no-prefix, medium `%o`=`777`); full gate 366/366; stevie startup screen BYTE-IDENTICAL 152 B (libstub-free default vs `--libstub` anchor, the §7r/§7v rendered-screen compare); `make check` green; the §7y/§7z support caches auto-invalidate (the stamp hashes `NL_SUPPORT`, now listing `dos_printf.c`).  STRATEGY unchanged (COPY/ADD, NEVER MUTATE): `dos_printf.c` is all-new, the build-script change is a one-line `NL_SUPPORT` swap, `test-dos.sh` is a one-case unpin; `libstub.asm`/`libstub_to_exe.py`/newlibc's `printf_wrappers.c`/every other toolchain file are UNTOUCHED, so MP/the libstub anchor/every existing gate provably can't regress.  With setjmp (§7x), the DGROUP heap (§8a), and now printf `%p`/`%o` (§8b) all libstub-free, libstub is no longer a correctness dependency of ANY gate probe — `--libstub` survives only as the optional equivalence anchor.  **§8c (2026-06-16) MOVED MicroPython ONTO THE LIBSTUB-FREE RUNTIME — the byte-frozen regression corpus now builds AND runs with NO libstub, byte-exact vs host `python3` in BOTH DOSBox and on real-hardware-equivalent MAME victor9k; `tools/build-micropython.sh --no-libstub` is the new path (OPT-IN — the default still builds the libstub corpus, byte-identical at image 751,664 / body 731,088), `make check` green, no compiler/qbe/emit/minic source touched (→ no emit audit).**  §8c's brief was "retire `libstub_to_exe.py`'s printf", but the premise that "nothing depends on it" was FALSE — `build-micropython.sh` builds MP directly through `libstub_to_exe.py` and MP's gc'd map links `_far_printf`/`_far_sprintf`/`_printf`/`_sprintf` from `libstub_exe.obj`, so libstub's printf engine is in MP's frozen body (and the `--libstub` anchor + fallbacks + probe scripts use it).  Per user direction ("update MP, don't worry about the byte-freeze") the real goal was to move MP off libstub — the §7r/§7v stevie migration scaled up.  **THE TRUE GAP WAS TINY (not the 124 symbols the map shows — `libstub_exe.obj` is monolithic so `--gc-sections` can't strip its passengers like `_mouse_*`/`_putpixel`/`_getch`):** an empirical relink showed only **(1) `___builtin_clzl` (103 refs) + `___builtin_clz`/`expect`/`unreachable`** (the one set libstub provided that the libstub-free runtime lacked) and **(2)** the `dos_shim.c` FILE/syscall surface (lost only to its `_main` collision).  MP's reader is fd-based (`read(fd)` via `mp_reader_new_file_from_fd`), NOT stdio `FILE`, so the §7s `FILE`-ABI landmine does not apply.  **FIX (COPY/ADD, NEVER MUTATE):** NEW `minic/dos/builtins_rt.asm` (the 4 `__builtin_*` COPIED VERBATIM from `libstub.asm`, near form, far-rewritten by `near_to_far_rt.py --seg-name=BUILTINS_RT_TEXT` like `qbe_rt.asm`; `libstub.asm` UNTOUCHED) + an OPT-IN `--no-libstub` branch in `build-micropython.sh` (the §7v far-data runtime recipe + `builtins_rt` + MP's `main()` renamed to `newlibc_test_main` via `-Dmain` so `dos_shim.c`'s `main` wrapper is the crt0 entry — `vfs_init()` is a no-op on the DOS path).  `dos_shim.c` stays PRISTINE: a `-DNO_SHIM_MAIN` variant hit a minic emission quirk (removing the trailing `main()` drops the file-scope FILE-layer statics `shim_files`/`_impure_ptr` from the TU — a latent minic bug noted, not fixed), so the `-Dmain` rename keeps `main()` present and sidesteps it.  VERIFIED: libstub-free MP compact links clean (110 modules, 0 undefined, image 710,352 / body 689,760 — smaller, passengers gone); a DOSBox-sized build (571,600 B) and the default-heap build (710,352 B) both ran a 13-line smoke test (language + FLOAT + `math` + recursion + ZeroDivisionError) BYTE-EXACT vs host `python3` in DOSBox AND on MAME victor9k respectively; the DEFAULT (libstub) build re-confirmed BYTE-IDENTICAL frozen corpus (751,664 / 731,088); `make check` green.  libstub is now retired as MP's runtime *capability* (opt-in, verified); `libstub_to_exe.py` survives as MP's DEFAULT runtime + the `--libstub` equivalence anchor + the fallbacks.  **§8d next:** optionally FLIP `build-micropython.sh`'s default to `--no-libstub` and REBASELINE the regression corpus (frozen body → ~689,760; update every "731,088 byte-identical" reference + the MP byte-compare tooling/docs) — deferred as it rebaselines the project's regression baseline (the user's call; the opt-in path is proven so the flip is de-risked); OR a carried compiler track (the aoa sub-gaps; the huge pointer EQUALITY flat-compare, the §7u relational fix's latent sibling; the minic file-scope-statics-need-a-trailing-main quirk — all await a consumer).  **§8d (2026-06-16) FLIPPED MicroPython's default to libstub-free and REBASELINED the regression corpus — `tools/build-micropython.sh` (no flag) now builds the libstub-FREE MP (compact default-heap body 689,760, was 731,088 under libstub); `--libstub` is the opt-out equivalence anchor (still body 731,088, verified intact); no compiler/qbe/emit/minic source touched (→ no emit audit).**  The §8d change is a one-line default flip (`NO_LIBSTUB=0`→`1` in `build-micropython.sh`) + doc/comment rebaseline; the produced binary is byte-IDENTICAL to §8c's already-Victor-verified `--no-libstub` compact build, so no new Victor run was needed — VERIFIED: default compact = body 689,760 (reproducible), `--libstub` compact = body 731,088 (anchor intact), the default small-heap build runs the §8c smoke test (language + FLOAT + `math` + recursion + ZeroDivisionError) BYTE-EXACT vs host `python3` in DOSBox.  The 731,088 baseline was DOCUMENTATION-only (no hardcoded assertion in any script; `recompile-mp-tu.sh` is data-driven from `/tmp/mp_objs.txt`, so it adapts automatically).  Historical "MP body 731,088 byte-identical" session records (§4t…§8a) are LEFT as-is — accurate point-in-time records of the libstub corpus.  `libstub_to_exe.py` now survives only as the `--libstub` MP anchor + the `build-{sprintf,int86x,divmod32}-probe.sh` scripts + the `build-example.sh`/`build-stevie.sh` `--libstub` anchor + the tiny/.COM fallbacks.  **§8e (2026-06-16) CLOSED the huge-pointer EQUALITY flat-compare — the latent sibling of the §7u relational fix; `==`/`!=` of two huge pointers that denote the SAME linear byte through DIFFERENT seg:off normalisations now compare correctly; test-dos 366 → 367, the fix is an MHuge-gated `minic.y` frontend change → no emit audit, MP compact body 689,760 BYTE-IDENTICAL → no Victor run, `make check` green.**  §7u routed huge pointer RELATIONAL compares (`p < q`, `p <= q`) through `_qbe_huge_cmp` because the flat 32-bit `cultl`/`csltl` of two seg:off words is only monotonic-in-linear-address when BOTH operands are NORMALISED; it explicitly LEFT the equality case unfixed (the sole live consumer, `_sbrk`, only does `== NULL` = 0:0 = linear 0, which is fine flat).  The equality gap is the SAME defect: the flat `ceql`/`cnel` compares the raw 32-bit (seg<<16)|off words bit-for-bit, so an UNNORMALISED operand (a bare symbol address whose offset can exceed 0xF) wrongly compares UNEQUAL to the NORMALISED pointer `_qbe_huge_add` returns for the SAME byte (a pure FALSE-NEGATIVE — a genuinely different address can never alias).  **FIX (minic.y, the `Binop` default arm, ~25 lines, mirrors the §7u relational block):** under `memmodel == MHuge` an equality compare (`o == 'e' || o == 'n'`) with BOTH operands non-function pointers now routes through `_qbe_huge_cmp(p,q)` (= signed `linear(p)−linear(q)`, the SAME normalisation-invariant helper §7u uses) and tests its sign: `p == q ⟺ ceql cmp,0`, `p != q ⟺ cnel cmp,0`.  Linear equality is exactly C11 6.5.9 pointer equality on the flat 8086 huge model, and it stays correct for NULL too (`0:0` → linear 0).  MHuge-gated, so compact/large/near — including the MP byte-compare — keep the flat `ceql`/`cnel` and are untouched.  Gated by the all-new `huge_ptreq_probe` (`minic/dos/examples/huge_ptreq_probe.c`, :huge) — bug-loud: it builds two pointers to the same linear byte through different normalisations (a `(char *)constant` raw/unnormalised cast vs the normalised result of `+ 0` through `_qbe_huge_add`) and asserts `==`/`!=`; on the UNFIXED compiler the cross-normalisation cases print `eqconst=FAIL`/`neconst=FAIL`/`eqarith=FAIL`/`nearith=FAIL` (verified by reverting the fix), while the address-distinct and NULL cases stay correct.  Every existing huge probe (huge_norm, hugeprobe, gc_bigheap, the caddr family, …) still passes — their `p==q`/`p!=NULL` codegen now goes through `huge_cmp` but yields the SAME correct answer.  STRATEGY: the only source change is the MHuge-gated minic.y arm + the new probe/golden + one gate entry; the COPY/ADD-NEVER-MUTATE libstub-free toolchain is untouched, so MP/stevie/every existing gate provably can't regress.  With this closed, the §7u huge-normalisation family (relational §7u + equality §8e) is complete.  **§8f (2026-06-16) INVESTIGATED the carried "minic file-scope-statics-need-a-trailing-`main`" quirk (the §8c handoff's latent compiler track) and DISPROVED it — it is a NON-BUG; doc-only close, no source touched, gate UNCHANGED at 367/367.**  §8c reported that dropping `dos_shim.c`'s trailing `main()` (the abandoned `-DNO_SHIM_MAIN` experiment) dropped ALL file-scope statics (".ssa 0-vs-4 defs", "main gates static-data emission").  THREE proofs it does NOT reproduce: (1) `minic.y:10531-10548` emits file-scope data via an UNCONDITIONAL post-`yyparse()` loop over `gloname[]` — `main` is never consulted, so "0 defs" ⟺ `yyparse()!=0 → die("parse error")` ⟺ MALFORMED input; (2) exact reconstruction (`#ifndef NO_SHIM_MAIN`, real cpp `-D__ia16__ -DNO_LIBSTUB`, minic compact+medium) emits all 4 defs exit 0 BOTH ways; (3) truncating the no-`main` TU at all 40 top-level `^}` boundaries — every one parses OK, defs rise 0→2→4 with declared statics.  The §8c 0-vs-4 was a MEASUREMENT ARTIFACT (the `-DNO_SHIM_MAIN` wrap fed minic malformed C — an eaten brace — → parse-error → empty `.ssa`).  The `-Dmain=newlibc_test_main` rename remains right (solves the real `_main` collision).  No gate added (a meaningful one needs multi-TU link plumbing — `build-example.sh` is single-source — disproportionate for a non-bug).  ONE carried compiler track remains: the aoa sub-gaps (file-scope/static multi-decl array-first parse-error gap; plain `jmp_buf a, b;` multi-decl).

---

## 📍 Status & history pointers

- **[ROADMAP.md](./ROADMAP.md)** — current component status, Phase 5 close-out, the Phase 6 (newlibc) plan.  *(Updated 2026-06-11.)*
- **[NEXT_SESSION.md](./NEXT_SESSION.md)** — per-session engineering notes, newest first (§5c at top).  The detailed history of every bug and fix lives here and in git, not in this file.
- **[MICROPYTHON_PORT.md](./MICROPYTHON_PORT.md)** — the MP port reference.
- **[I8086_TARGET.md](./I8086_TARGET.md)** / **[i8086/README.md](./i8086/README.md)** — backend reference.
- Memory files (auto-loaded) carry the distilled bug-class lessons ([[…]] links).

## Open tracks (pick by appetite)

0. ~~**TOP PRIORITY — a REAL QBE BUG.** QBE SIGABRTs `assoccon` `gvn.c:210` on `(char*)&a[i]-(char*)&a[0]`~~ — CLOSED (§7f): minic `prom()`'s first PTR−PTR handler returned `LNG` unconditionally → a near `char*` diff was typed `l sub` of two `w` operands; after GVN forwarded the loadws, the `l sub` consumed a `w add` near-ptr def and `assoccon`'s width assert aborted.  Two fixes: (1) minic first handler now `ISFAR(l->ctyp)?LNG:INT` (near ptrdiff = `w`); (2) QBE `gvn.c:210` `assert`→`if (KWIDE(i2->cls) < KWIDE(i1->cls)) return;` (bail, never SIGABRT — no-op for valid IR).  Checked `upstream` FIRST: assoccon byte-identical, not a known fix.  Gated `ptrdiff_probe` small+medium+compact+large (bug-loud: unfixed = Abort trap 6), test-dos 309→313, MP body 731,088 identical, `make check` green.  NO QBE backend bug currently open.
1. ~~huge `_qbe_huge_add` ≥0x8000 variable-index gap (§4i)~~ — CLOSED (§7g): the helper itself was correct; the bug was the CALLER.  minic's Scale path (`prom()`, minic.y ~2209) UNCONDITIONALLY `sext`s a sub-long index before scaling, so an UNSIGNED `size_t` byte offset ≥0x8000 (e.g. MicroPython gc_alloc's `pool_start + start_block*16` on a >32 KB heap) reached `_qbe_huge_add` as a NEGATIVE 32-bit value and was added BELOW the object.  (compact/large never saw this because §4i's addfo/subfo read only the offset's low 16 bits, where `extsw`==`extuw`; huge adds the full 32-bit value to the 20-bit linear address, so the sign matters.)  Fix: under `memmodel == MHuge` the Scale path uses `widen_int_to_long` (picks `extuw`/`extsw` by source signedness) instead of `sext`; compact/large keep the uniform `sext`.  Gated `gc_bigheap_probe` :huge (was compact+large only; bug-loud: unfixed huge prints `direct=0` for b≥2048 + FAIL), test-dos 313→314, MP body 731,088 byte-identical (fix is MHuge-gated so compact unchanged), `make check` green.  minic-frontend change (not emit.c) → no emit audit.
2. ~~`jmp_buf bufs[6]` cross-frame longjmp (§4v)~~ — CLOSED (§7e): `jmp_buf` is `int[8]`, so `jmp_buf bufs[N]` is an array-of-array-typedef; minic's flat type system has no `int (*)[8]` and the array-declarator rules ignored the typedef inner dim, so `bufs[N]` was sized `int[N]` (12 B not 96) and `bufs[i]` was a scalar-int load (stride 2) not the row address `bufs+i*16` → all setjmps aliased bufs[0], cross-frame longjmp resumed the wrong frame.  Fix = `varh.aoa_dim` flag set at the 3 decl sites (file-scope/block-local/static-local), register `IDIR(elem)` with `N*D*sizeof(elem)` size; `mkidx` desugars `bufs[i]` → bare ptr-add `bufs+i*D` (no deref) so Scale gives the int* row addr (reuses far_ptr_offset_binop, composes with `bufs[i][j]`/`setjmp(bufs[i])`).  Gated `arr_jmpbuf_probe` medium+compact+large, test-dos 306→309, MP body 731,088 identical.  Bounded gap left: brace-init/multi-decl aoa forms still ignore it (no consumer).  **§7j CLOSED the parseable bounded forms** — block-local multi-decl `jmp_buf a[2], b[2]` (`emit_local_multi_decl_full` `'B'` now honors `g_td_arraydim`) + brace-init 2-D tables `row3_t t[2]={{…},{…}}` (new `mk_aoa_array_init` flattens braced rows into raw `'@'(+ V lin)` stores, wired into all four dcls/stmt × sized/unsized rules); `aoa_extended_probe` medium+compact+large, test-dos 317→320, MP body 731,088 identical, frontend → no emit audit.  **§8g CLOSED the remaining array-typedef-INSTANCE gaps** — a survey found the defect was WIDER than the §7j note: only block-local single (§7e) + bracketed multi (§7j) were correct; SIX shapes were broken (file-scope single/multi instance, file-scope array-FIRST `static jmp_buf fa[2], fb[2]` PARSE ERROR, block-local multi `jmp_buf a, b;`, static-local single).  Root cause for the mis-sized ones: only the block-local single-decl rule had the `g_td_arraydim > 0` branch; every other plain-instance/multi-decl site sized the ELEMENT type (int, 2 B scalar + `loadw`, not a 16-B array decay).  Fix (frontend minic.y, all g_td_arraydim-gated): helpers `emit_global_arr_instance` + `emit_global_sized_array` (the latter factored VERBATIM from the `[expr] ';'` rule → byte-identical) wired into the file-scope bare-`;` / `, ext_decllist` / multi-decl-emit funcs / static-local rules, plus ONE new production `typed_decl_rest: '[' expr ']' ',' ext_decllist ';'` (file-scope array-first, GAP1; also fixes plain-int `int counts[3], total;`).  Gated `aoa_instance_probe` medium+compact+large, test-dos 367→370, conflicts UNCHANGED 115, MP body 689,760 identical, frontend → no emit audit.  STILL bounded (NOT aoa-specific — a general grammar hole): function-local `static` MULTI-declarators (`static int x, y;` / `static int a[3], b;` / `static jmp_buf a, b;` inside a fn) are all parse errors — the `dcls STATIC type IDENT …` rules have no multi-decl production at all (affects plain int too).  **§8h CLOSED this** (frontend minic.y, all `g_td_arraydim`-gated → non-array-typedef base byte-identical to the existing single-decl rules): 3 new helpers near `emit_global_sized_array` — `emit_static_local_scalar_or_instance` (plain scalar OR array-typedef instance first declarator; mirrors the `dcls STATIC type IDENT ';'` body), `emit_static_local_sized_array` (aoa-aware sized array, like the stmt-scope `STATIC … '[' expr ']' ';'` rule), and `emit_static_local_rest_item` (one ext_decllist item, dispatched by op tag with uniform-* peeling via `ebase = (KIND(base)==PTR)?DREF(base):base` so `static char *p, *q;` makes both `char*` and `static char *p, c;` makes `p` a pointer + `c` a `char`; `F`/`G`/`H` proto items `varadd` with no storage, an unsized `A` item or a per-item initializer dies clearly) — wired into 4 new productions (`dcls STATIC type IDENT ',' ext_decllist ';'` + `dcls STATIC type IDENT '[' expr ']' ',' ext_decllist ';'` + the two statement-scope equivalents), each declarator emitted as its own mangled file-scope data global via `emit_static_local`.  Gated `static_multidecl_probe` medium+compact+large (bug-loud: on the UNFIXED compiler every probe function is a parse error → the build fails; exercises distinct storage + persistence + correct type/size for all six shapes + a statement-scope multi-decl, golden byte-identical across all three models), test-dos 370→373, conflicts UNCHANGED 115, MP body 689,760 BYTE-IDENTICAL (MP has no static multi-decls → the new branches never fire), `make check` green, frontend → no emit audit.  STILL bounded (no consumer): the INITIALIZED static multi-decl (`static int x = 1, y = 2;`) remains a parse error — a distinct, larger feature (per-item const-init folding into each data block + a separate `… '=' expr ',' init_decllist ';'` production family); the new productions reject a per-item initializer with a clear die.  With this closed, NO carried compiler track remains.  NOTE: a row-ptr-subtraction diagnostic during the §7e work surfaced the QBE `assoccon` SIGABRT (`gvn.c:210`) — pre-existing, NOT caused by the aoa fix (repros on plain `int a[6]`); CLOSED in §7f.  **§8i** then closed the INITIALIZED static multi-decl (`static int x = 1, y = 2;`): `ext_decl` already captured rest-item initializers, so it was a small additive frontend fix (test-dos 373→376, MP body 689,760 identical, no emit audit).  **§8j (Phase-6 "compile newlib proper", user-chosen)**: re-ran the §6a triage on the current phase3_newlib tree (now **53/66** TUs compile, up from 46) and fixed its LONE genuine codegen bug — **GCC extended-asm OUTPUT/INPUT operands** (`"=r"`/`"=m"`/`"r"`/`"m"` bound to a local) now work END-TO-END.  It was an incomplete feature: minic substituted the local as `[bp-%name]` but (1) QBE `promote` aborted on the output slot (read-never-stored), (2) input slots were the dual defect (forwarded into a register, alloc removed), and (3) i8086 emit printed the asm string verbatim — `%name` was never resolved to a frame offset (the never-gated `asm_clobber_test.c` confirms it never worked).  Fix (3 parts, additive): minic.y lowers the operand to the bare `%name`; a new QBE `mem.c` `asmvol(fn)` pass (before `markvol`) scans `Oasm` strings and marks the named alloc `vol=1` so `promote`/`coalesce` keep the slot in memory (inputs AND outputs, no promote-logic change); i8086/emit.c resolves `%name`→`[bp±N]` via `tmp->slot`.  Gated `asm_output_probe` small+medium+compact+large+huge (bug-loud: unfixed aborts at the QBE stage), test-dos 376→**381**, `make check` green, **emit audit clean (0 clobbers / 124,955 regions — i8086/emit.c changed, audit required + run)**, MP compact body 689,760 BYTE-IDENTICAL.  The triage qbe failure bucket went **4 → 0** (the 4 drivers display/keyboard/pic/sasi no longer abort and advance one stage to the nasm bucket); they STILL don't compile end-to-end because their inline asm is gas/AT&T syntax (minic passes asm templates through verbatim — no syntax translation), the §6a "driver asm needs per-target porting" conclusion — the `bm_*.c` ports already did this.  So the fix is a real codegen-correctness gain and a prerequisite for any ported driver, but the remaining 13 newlibc fails (12 nasm-bucket gas-syntax TUs + the `interrupts.c` ISR-fn-pointer-param parse gap + 6 Watcom-`_asm{}` dos_tests) are all PORTING work, not compiler bugs.  NO carried compiler track remains.  **§8k (user-chosen "gas→nasm driver porting", in-place in `~/projects/newlibc/phase3_newlib`)**: PORTED the entire **nasm-failure bucket** — the 12 gas/AT&T inline-asm TUs (6 drivers console/display/keyboard/pic/sasi/timer + libgloss/board_init + 5 tests crtc/es_ss_diagnostic/font_ram/pic/serial_debug) now compile END-TO-END under minic (triage PASS 53→**65** at small AND medium, nasm bucket **12→0**); **NO qbe compiler source touched** (minic/qbe/emit byte-identical → `make check` green, test-dos/MP/stevie provably unaffected, no emit audit, no MP byte-compare).  Introduced the **`-D__MINIC__`** convention (added to `build/newlibc-triage/sweep.sh`'s `clang -E`; kept `-D__ia16__` for `MK_FP`), so upstream forks its asm `#if defined(__MINIC__)` (Intel/nasm) `#else` (gas) — ia16-gcc/Watcom take the unchanged `#else`, BYTE-IDENTICAL.  Idioms: interrupts.h SAVE_ES/RESTORE_ES → no-op (§6e/§6i/§6y, ISR ABI owns ES) collapsing get_interrupt_vector to a far-ptr IVT read (unblocked 4 tests + every driver, 53→57); `intel_dev_write_byte`/`write/read_crtc_reg` → `HW_WRITE_BYTE`/`HW_READ_BYTE` volatile far MMIO; `pic_delay` → `jmp short $+2` ×2; flags-save/cli → Intel `pushf`/`pop word %0`/`cli` (§8j operand → `[bp-10]`).  The es_ss_diagnostic lesson: §8j operand resolution matches a local by NAME → needs a UNIQUELY-named slot-resident temp, so a macro reusing a local name across call sites collides (fixed by per-register helper fns).  9 newlibc files, +127 lines, all additive + `__MINIC__`-gated, UNCOMMITTED on the separate `~/projects/newlibc` repo.  COMPILE-correctness only — NOT yet run bare-metal (upstream drivers aren't wired into an image; the build links bm_*.c).  Still no QBE/minic codegen bug, still NO carried compiler track; the 8 minic-parse-bucket TUs (interrupts.c ISR-fn-ptr param + vshell + 6 Watcom-`_asm{}` dos_tests) remain a SEPARATE porting track.  (The §8k port is committed on newlibc branch `minic-asm-port` @ `5b6b261`, not merged to `main`.)  **§8l (user-chosen "functional bare-metal gate")**: WIRED newlibc's OWN `drivers/timer.c` into a bare-metal image IN PLACE OF the hand-mirrored `bm_timer.c` and RAN it on MAME victor9k — the **first proof a §8k-translated upstream phase3 driver RUNS bare-metal** (§8k was compile-only).  New test `minic/dos/newlibc/timer_upstream_bm.c` (modeled on §6d `timer_bm.c`) drives the UPSTREAM `timer_init`/`timer_get_ticks`/`timer_delay_ms`/`timer_get_frequency`/`timer_tick_handler` API; the ISR plumbing is the generic compiler-emitted ABI (a local `__attribute__((interrupt))` `timer_isr` routes each IR2 tick to upstream `timer_tick_handler` + the specific EOI, installed via an inline IVT write mirroring `bm_install_isr`; `bm_pic_init()` re-inits the 8259).  NOTHING from `bm_timer.c` linked (map: every `timer_*` from `mod=timer.obj`).  TWO build-glue changes to `tools/build-newlibc-baremetal.sh` ONLY (additive): (1) added `-D__MINIC__` to the `clang -E` line (the §8k convention on the real bare-metal path) → `timer.c`'s `intel_dev_write_byte` takes the Intel `HW_WRITE_BYTE` fork (verified codegen: `mov es,0xE000`+`es:[bx]` in control-then-count order, 0 gas leftovers); harmless for existing tests (no linked `bm_*.c`/portable TU tests `__MINIC__`, the §8k-ported upstream `.c`/`interrupts.h` aren't linked — the §6y `interrupts.h` shadow wins the include path; `timer_bm` golden byte-unmoved); (2) a SUPPORT_TUS rule — a non-test-host, non-`bm_stdio` program including upstream `"timer.h"` links `$NL/drivers/timer.c`, guarded `[ "$TESTHOST" = 0 ] && ! grep -q 'bm_stdio\.h'` because test-host/`bm_stdio` tests pull `bm_timer.c`+`bm_shim.c`'s `timer_*` aliases (would duplicate-collide; verified `pic_test` links only `bm_timer.obj`).  Adds a `timer_get_frequency()==100` phase (an upstream fn `bm_timer.c` lacks).  FIRST-RUN PASS on MAME (8 phases incl. live ticks + 1500 ms sustained interrupts + cli-freeze); golden deterministic ([50..80] window absorbs MAME's 125 KHz ch2 clock), toolchain-stable; bare-metal ONLY; small; battery 41→42.  Only build scripts + new test + golden + battery entry changed → compiler byte-identical → `make check` green, no emit audit, no MP byte-compare, test-dos UNCHANGED.  NO QBE/minic codegen bug, NO carried compiler track.  **§8m (user-chosen "more upstream drivers bare-metal", the §8l pattern applied to a SECOND driver)**: WIRED newlibc's OWN `drivers/display.c` (+ `drivers/font_data.c`) into a bare-metal image IN PLACE OF the hand-mirrored `bm_display.c`/`bm_font_data.c` and RAN it on MAME victor9k — the **second proof a §8k-translated upstream phase3 driver RUNS bare-metal** (after §8l's timer.c).  New test `minic/dos/newlibc/display_upstream_bm.c` (modeled on §6e `display_bm.c`) drives the UPSTREAM `display_init`/`display_clear`/`display_puts`/`display_putc`/`display_putc_at`/`display_set_cursor`/`display_get_cursor`/`display_scroll` API and verifies every effect by reading it back from the machine — VRAM words through a far pointer to F000:0000 (local `screen_word`/`read_cell`, the `(attr<<8)|(c+0x60)` encoding), the font from its 0000:0C00 RAM home (vs the upstream `victor_font[]` table from `font_data.c`), the cursor via the upstream `display_get_cursor` (CRTC R14/R15) — over the serial console.  The display driver is **pure polled MMIO (no interrupts)**, so unlike §8l's timer there is NO ISR plumbing; the §8k port of `display.c`'s `write_crtc_reg`/`read_crtc_reg` → `HW_WRITE_BYTE`/`HW_READ_BYTE` (verified codegen: `mov es,0xE800`+`mov byte es:[bx]`, 0 gas leftovers) is exercised live.  NOTHING from `bm_display.c`/`bm_font_data.c` linked (map: every `display_*` from `mod=display.obj`, `victor_font` from `mod=font_data.obj`).  ONE build-glue change to `tools/build-newlibc-baremetal.sh` (additive, mirroring §8l's timer rule): a non-test-host, non-`bm_stdio` program including upstream `"display.h"` (NOT `bm_display.h`; the leading-quote `'"display\.h"'` pattern does not match `"bm_display.h"`) links `$NL/drivers/display.c` + `$NL/drivers/font_data.c`, guarded `[ "$TESTHOST" = 0 ] && ! grep -q 'bm_stdio\.h'` because test-host/`bm_stdio` tests pull `bm_display.c`+`bm_shim.c`'s `display_*` aliases (would duplicate-collide; verified `font_test` links only `bm_display.obj`/`bm_font_data.obj`).  FIRST-RUN PASS on MAME (9 phases: init/cursor-home/blank-word/font-readback/puts+advance/putc_at-attr/newline-blank/tab+backspace/bottom-row-scroll); golden deterministic booleans only, toolchain-stable; bare-metal ONLY (no Victor VRAM on the DOS host); small (8581 B code); 20 s budget; battery 42→43.  Verified `display_upstream_bm` `[ok]` AND `display_bm` `[ok]` (the `-D__MINIC__` from §8l didn't move the mirror golden).  Only build scripts + new test + golden + battery entry changed → compiler byte-identical → `make check` green, no emit audit, no MP byte-compare, test-dos UNCHANGED.  NO QBE/minic codegen bug, NO carried compiler track.
3. ~~minic static-init float const-expr folding~~ — CLOSED/CORRECTED (§6z): folding was never broken; the real bug was missing `CONST`/`vol_qual` `TFLOAT`/`TDOUBLE` grammar productions (`const float`/`volatile double` etc. were parse errors).  Fixed + gated (`const_float_init_probe`); also unblocks MICROPY_PY_MATH_CONSTANTS (MP parked, so not enabled).
4. ~~Small-model setjmp/longjmp (near env)~~ — CLOSED (§7a): `NEAR_SETJMP_EXE` in `libstub_to_exe.py` (near `_setjmp`/`_longjmp`, 6-word jmp_buf, no CS word, near `ret`); gated small (`setjmp_probe` + `setjmp_clobber_probe`), test-dos 298→300, MP body 731,088 identical.
5. ~~Multi-decl items skip block_scope_decl (loud "double definition")~~ — CLOSED (§7b): a MULTI-declarator block-scope local (`T a, b;`, the first item included) shadowing a global/function/enum/different-typed outer local died "double definition" because `emit_local_multi_decl`/`_full` (+ the `int a=1,b=2;` rule's tail loop) called `varadd` directly, bypassing the §6a/§1k rename.  Fix routes every storage declarator through `block_scope_decl`; gated `multi_decl_shadow_probe` small+medium, test-dos 300→302, MP body 731,088 identical.
6. Kw spill-slot sharing — frame-size lever, no consumer pain.
7. ~~Stmt-context array-first multi-decl `int arr[3], *counter;` does not parse~~ — CLOSED (§7c): the stmt-context multi-decl production was only `type IDENT ',' ext_decllist` (bare leading IDENT); an array-decorated first declarator MID-BLOCK had no production (the dcls/function-top form already parsed).  Fix adds `type IDENT '[' expr ']' ',' ext_decllist ';'` mirroring the dcls array-first rule (kr_array_node + emit_local_multi_decl_full), deferring the init chain as an Expr stmt; conflicts unchanged.  Gated `arrayfirst_multidecl_probe` small+medium, test-dos 302→304, MP body 731,088 identical.
8. ~~far static-DATA-ptr reloc (§1g)~~ — CLOSED (§7h): under a far-data model a static initializer holding `&global` (`int *p=&g;`, `int *mid=&arr[2];`, `char **e=words;`) is a 4-byte far pointer, but `asm_to_omf.py` emitted `dd _sym` (offset-only, segment word 0) → wrong-segment far deref.  The `.long _sym`→`dw _sym / dw seg _sym` split (FIX 3) was gated `split_sym_long = far_data or model=='medium'` and `far_data` requires `--far-static-data` (only MP's `MP_SPLIT_STACK` layout sets it) — so a DEFAULT compact/large/huge build (how the gate builds probes) skipped the split.  The split is about whether DATA POINTERS ARE FAR (every far-data model), independent of the `--far-static-data` section ROUTING `far_data` controls.  Fix: `split_sym_long = model in ('compact','large','huge','medium')` (`far_data` untouched — it still drives section/class routing).  Gated `static_sym_init_probe` +:compact+large+huge (was small+medium; bug-loud: unfixed compact prints `4194/4192` + Illegal-byte-sequence `%s` deref), test-dos 314→317, MP body 731,088 byte-identical (MP uses `--far-static-data` → split already fired), `make check` green.  Toolchain script (not emit.c) → no emit audit.

## House rules (process invariants that cost real time to relearn)

- **Gate before trust:** every fixed bug gets a reduced `minic/dos/examples/*_probe.c` wired into `tools/test-dos.sh` (bug-loud — verify the probe fails on the unfixed code) BEFORE the behavior is relied on.
- **minic staleness trap:** top-level make skips minic.y rebuilds.  Always `rm -f minic/minic && touch minic/minic.y && make minic/minic` and verify the .ssa changed before trusting a gate run.  See [[minic-make-staleness]].
- **After ANY i8086/emit.c change:** run `tools/run-emit-audit.sh` (the §4y standing tool — exact-liveness CHK markers + symbolic checker; 369 files / 117k regions / 0 violations is the baseline; ISR ret regions are tagged `live=isr` and skipped; the corpus CACHES probe asm — rm a probe's `build/chk-corpus` entries after changing its codegen).
- **After ANY toolchain change:** rebuild MP compact (`tools/build-micropython.sh --model=compact`, needs a far-data model since MP_SPLIT_STACK=1) and byte-compare the body (default = libstub-free, body 689,760 since §8d; `--libstub` anchor body 731,088).  Byte-identical ⇒ no Victor run.
- **Victor-bound probes MUST print per-phase progress** — a 5 MHz 8088 makes silence unreadable (a 700 s "hang" was just slowness).
- **Victor harness is deterministic** — empty serial output means a host-invocation mistake or a real hang, never "MAME flakiness".  Run `tools/run-victor-sasi.sh` foreground.
- **DOSBox fast loop for MP:** `MP_HEAP_SIZE=8192 MP_HEAP2_SIZE=12288 MP_STACK_SIZE=16384` → ~600 KB image that loads in DOSBox; PROG.PY beside the exe.
- **miniyacc gotchas:** no standalone `/* … */` comment between a `;` and the next production head; action-body comments must avoid `'` `/` `[` `]`.
- **Grammar conflict baseline:** 115 shift/reduce, 0 reduce/reduce.  Any change to that count needs justification.
- **Commit at green-gate milestones** to master without asking; stage only the session's files; no push/PR unless asked.

## Build & test quick reference

```sh
make check                      # QBE SSA tests
tools/test-dos.sh               # the 274-entry per-model runtime gate (DOSBox)
tools/build-example.sh --model=<tiny|small|medium|compact|large|huge> <file.c>
tools/build-stevie.sh --exe     # stevie editor
tools/build-micropython.sh --model=compact   # the MP regression corpus
tools/run-victor-sasi.sh <exe>  # real-hardware-equivalent MAME victor9k run
tools/build-newlibc-baremetal.sh hello_bm    # bare-metal raw binary @ 0x3000
tools/run-victor-baremetal.sh <bin>          # MAME Lua-loader bare-metal run
tools/test-victor.sh            # slow on-target gate (MAME; incl. bare-metal hello)
tools/run-emit-audit.sh         # emit-bracket liveness audit (after emit.c changes)
```

---

## Repository Information

**Repository:** https://github.com/pauldevine/qbe  (branch: master; PRs #23 upstream rebase, #24 §3p–§4y, #25 §5c, #26 §6a–§6b, #27 §6c–§6h)
**Upstream:** `upstream` remote → c9x.me/qbe.git — check upstream first for generic spill/SSA/regalloc bugs.  In sync through `e786f06` (2026-06-11).
**Related trees:** `~/projects/newlibc` (Phase 6 target), `~/projects/micropython` (dos8086 port), `/Applications/dosbox.app`, MAME victor9k.

This project is developed by Paul Devine with assistance from Claude (Anthropic).
